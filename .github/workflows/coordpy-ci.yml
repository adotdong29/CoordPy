name: coordpy-ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sdk-contract-tests:
    name: SDK contract tests (${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install CoordPy (editable)
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -e ".[dev]"
      - name: CoordPy SDK contract + provenance + extensions tests
        run: |
          python3 -m unittest \
            vision_mvp.tests.test_coordpy_public_api \
            vision_mvp.tests.test_coordpy_provenance \
            vision_mvp.tests.test_coordpy_extensions \
            vision_mvp.tests.test_coordpy_runtime \
            vision_mvp.tests.test_phase45_product \
            -v
      - name: Smoke-test console scripts
        run: |
          coordpy --version
          coordpy --profile local_smoke --out-dir /tmp/coordpy-ci-smoke
          coordpy-ci --report /tmp/coordpy-ci-smoke/product_report.json \
                   --min-pass-at-1 1.0

  build-distributions:
    name: Build sdist + wheel
    runs-on: ubuntu-latest
    needs: sdk-contract-tests
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install build
        run: python3 -m pip install --upgrade pip build
      - name: Build
        run: python3 -m build
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: coordpy-dist-${{ github.sha }}
          path: dist/*

  release:
    name: Release (on tag)
    runs-on: ubuntu-latest
    needs: build-distributions
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install build
        run: python3 -m pip install --upgrade pip build
      - name: Build
        run: python3 -m build
      - name: Upload to release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*
          body: |
            CoordPy SDK release.
            See CHANGELOG.md for details.
            Contract-tested at `SDK_VERSION = coordpy.sdk.v3.43`.
