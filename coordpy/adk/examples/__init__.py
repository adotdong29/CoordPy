"""coordpy.adk.examples — runnable, useful ADK example apps.

These are real, self-contained agents (not benchmark harnesses). Each runs
hermetically by default (a built-in deterministic backend, no network/model)
and against a real backend when you pass ``model=coordpy.llm_backend.backend_from_env()``.
"""
