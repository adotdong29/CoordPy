"""Worked examples of CoordPy extensions.

These are real, runnable extensions that exercise the extension
protocols end-to-end (``test_coordpy_extensions.py`` imports and runs
them). Treat them as templates — copy one into an external package,
drop in an ``entry_points`` block in its ``pyproject.toml``, and
CoordPy discovers it via ``importlib.metadata``.
"""
