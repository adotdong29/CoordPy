# W114 — Milestone summary (bounded-ceiling registration + certification-supply blocker; $0 NIM)

**One line:** W114 registered the hardened bounded ceiling as the honest
code-superiority FLOOR AND established — from primary sources + a NIM-free
per-model certification layer — that **no reachable model stronger than Maverick
is certifiably contamination-resistant on the latest real instrument**, because
the resistant FUNCTIONAL instrument frontier (Apr-2025) has aged out relative to
the reachable frontier-model class (cutoffs officially undisclosed). **No pilot
earned; $0 NIM. W89 + W105 remain the only two retirements.**

---

## The three lanes

### Lane α — bounded-ceiling registration (NIM-free; mandatory; the truth floor)

Registered the W113 outcome as the honest code-superiority FLOOR across the
canonical truth surfaces (`docs/RESEARCH_STATUS.md` Last-touched;
`docs/THEOREM_REGISTRY.md` Last-touched + claim-row + a new `## W114` section;
`docs/CONSOLIDATED_CODE_RETIREMENT_NARRATIVE_V1.md`;
`docs/HOW_NOT_TO_OVERSTATE.md`; new `docs/CONTAMINATION_CONTROL_FRAMING_W114_V1.md`;
`CHANGELOG.md`). Boundedness impossible to miss: **exactly TWO retirements** (W89
base HumanEval +5.56 pp; W105 HumanEval+ +7.00 pp), BOTH `meta/llama-3.3-70b-
instruct` @ 70B, contamination-EXPOSED-HumanEval-family; contamination-RESISTANT
same-budget code superiority is **0 clean across BOTH 70B and Maverick scales**;
the W112 +10 pp was EXPOSURE. Treated as the truth floor the new axis must beat —
NOT surrender (`W114-T-BOUNDED-EXPOSED-CODE-CEILING-REGISTERED`).
(README untouched — it carries no per-milestone research claims.)

### Lane β — genuinely different axis: certification-supply analysis (possibly LIVE)

Verified from PRIMARY sources (2026-05-29): the latest LiveCodeBench release is
`release_v6` (HF file tree — `test6.jsonl` is the highest-numbered; no `test7`+;
FUNCTIONAL subset 63 problems, all 2025-01-11..2025-04-05, frontier 2025-04-05);
Llama-4-Maverick's cutoff is **Aug-2024 KNOWN** (official Llama-4 model card),
and Qwen3-Coder-480B / DeepSeek-V4-pro / Mistral-Small-4-2603 all have
**OFFICIALLY UNDISCLOSED** cutoffs. The certification gate (C1 KNOWN ∧ C2 ≥30
functional resistant ∧ C3 reachable-stronger-comparable ∧ C4 not-already-settled)
returns **`NO_CERTIFIABLE_STRONGER_MODEL`** (decision CID `258b6ed7…`): Maverick
is the only KNOWN cutoff and is already SETTLED (W113 resistant FAIL ⇒ redundant);
every stronger-than-Maverick frontier model is C1-blocked (UNKNOWN) and, where
estimable, C2-blocked (cutoff at/after Apr-2025). **$0 NIM**, no pilot earned —
the resistant-instrument frontier has aged out relative to the model frontier
(`W114-L-RESISTANT-INSTRUMENT-FRONTIER-LAGS-MODEL-FRONTIER-CAP` +
`W114-T-STRONGER-MODEL-CUTOFFS-OFFICIALLY-UNDISCLOSED`).

### Lane γ — per-model certification layer + graphify (NIM-free; mandatory)

Built `coordpy.stronger_model_cutoff_certification_v1` (explicit-import-only;
imports the W113 registry + `partition_resistant_v1` + the tier-2 ranking, no
duplication) + `scripts/run_w114_stronger_model_certification_v1.py`. The script
re-verifies the pinned instrument month histogram against the SHA-pinned corpus
(`sha_ok` + `histogram_match` ✓) and asserts the W113↔W114 cutoff-confidence
consistency guard. 12 new tests incl. a falsifiability test (a synthetic
KNOWN-cutoff, not-settled model on a synthetic post-cutoff instrument DOES
certify — the rule is not hard-wired to no-go). graphify refreshed from HEAD at
start (`eb8d3ce`) + close; `explain`/`path`/`affected`/`query` used for file
selection + dependency checks. Extends the W113 tier-2 readiness into a real
per-model certification layer with the exact dated spend gate.

---

## Truth surface after W114

* **Confirmed retirements: still exactly TWO** — W89 + W105, both Llama-3.3-70B
  @ 70B, contamination-EXPOSED-HumanEval-family. W114 adds NONE, retires NONE.
* **Resistant superiority: 0 clean across BOTH scales** (unchanged from W113;
  now REGISTERED as the ceiling).
* **Certification supply: BLOCKED.** No reachable stronger-than-Maverick model is
  certifiable on the latest real instrument; the blocker is precise + dated.
* **Contamination-confound: UNCHANGED** (W114 tests certification supply, not the
  confound; STRENGTHENED-not-proven, per W113).
* Still NOT cross-class, NOT a resistant win, NOT MBPP-family, NOT cross-modal,
  NOT "context solved".

## Entitlement delta

NOT entitled to a stronger superiority/retirement claim. The bounded
two-retirement contamination-EXPOSED-HumanEval-family-at-70B claim is now the
REGISTERED ceiling, and W114 SHARPENS the boundary by establishing (from real
data) that the resistant-instrument supply has fallen behind the reachable model
frontier — a precise spend gate, not a vague "needs more work".

## W115 (made obvious by W114)

W115 fires only when a resistant FUNCTIONAL instrument with ≥30 problems dated
strictly after a reachable frontier model's KNOWN cutoff exists: (a) a future LCB
`release_v7`+ with post-Apr-2025 functional problems (operator-fetched +
SHA-pinned + loader-admitted) AND (b) a reachable stronger-than-Maverick model
disclosing a KNOWN cutoff < those problems. Until then the bounded ceiling STANDS
and resistant-code NIM is BLOCKED. `COO-9` stays lead.

## Discipline / boundary

* 24th consecutive preflight/earn-discipline validation (W93–W114): runbook
  (`docs/RUNBOOK_W114.md`) locked before any NIM; the no-go branch is
  pre-committed by the rule, so the $0 spend is discipline, not omission.
* Stable boundary preserved: `0.5.20` / `coordpy.sdk.v3.43`; no PyPI;
  `coordpy/__init__.py` untouched; 1 new explicit-import-only module + 1 script;
  12 new W114 tests + 30 reused W113 tests pass.
* `COO-9` stays the lead path.

## Anchors

`docs/RUNBOOK_W114.md`, `docs/RESULTS_W114_STRONGER_MODEL_CERTIFICATION_V1.md`,
`docs/CONTAMINATION_CONTROL_FRAMING_W114_V1.md`,
`docs/FRONTIER_RELEVANCE_AUDIT_W114_V1.md`,
`results/w114/certification/certification_verdict.json`.
