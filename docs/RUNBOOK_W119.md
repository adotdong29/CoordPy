# RUNBOOK — W119

**Official ICPC public-package resistant battlefield CONSTRUCTION (pivot to a source
family that already ships the executable grader) + primary-source stronger-model
certification lane in parallel + one clean pilot ONLY if the package-based instrument is
actually admitted AND certifiable.**

> Locked **2026-05-30**. A gated milestone with THREE lanes — **NOT another LCB-packaging
> / provenance blocker memo, NOT another exposed rerun, NOT a "check if release_v7 exists
> yet" snapshot, NOT a bounded-context / compaction / token-compression job.** No NIM was
> spent this milestone (the pilot was not earned; § 6/§ 7), so the pre-NIM lock is
> satisfied; the no-go branch is pre-committed by the RULE (the P1..P8 + ≥30-slice bar are
> encoded in `coordpy.coordpy_icpc_public_functional_v1`, which decided the outcome
> deterministically), so the $0 spend is discipline, not omission. `ultracode` stays OFF.
> `COO-9` stays the lead path.

---

## The one question W119 answers

W118 (live, 2026-05-30) STOPPED waiting for a packaged `release_v7` and BUILT a
CoordPy-owned post-v6 functional-identity manifest of **894 official Codeforces problems**
— proving the date/identity axis is officially constructible at scale — then proved the
executable functional **GRADER is ABSENT family-wide** across the LiveCodeBench source
family (Codeforces API has no test field; LeetCode hidden tests deliberately private;
AtCoder system tests Dropbox-only). The instrument was **identity-admissible but NOT
pilot-admissible**: grader-blocked.

**W119 makes the aggressive move W118 set up:**

> **Stop trying to extract a grader from a grader-LESS source family. Move to an official
> source family that ALREADY SHIPS the executable grader artifact — the official ICPC
> problem-package family (`github.com/icpc`) — and build a clean contamination-resistant
> battlefield there. Does that family yield an admitted, grader-clean, ≥30-task
> post-cutoff resistant pass-fail battlefield AND a certifiable model?**

This is a NEW, explicitly **CoordPy-OWNED** instrument line
(`coordpy_icpc_public_functional_v1`). It is **not** LiveCodeBench, **not** the W118
Codeforces line, and it does **not** smuggle hand-curated problems. The lane is **not
complete if it only says "ICPC looks promising"** — it must either build a real admitted
official-package battlefield or prove **precisely**, machine-checkably, why the
official-package family still cannot yield a *pilot-runnable* one.

---

## Linear

* **`COO-9`** (High, Todo) — "Build a second code benchmark battlefield with lower ceiling
  pressure" — stays the **lead path**. Parent epic **`COO-6`**.
* **`COO-42`** = W118 (Done). Its close pre-commits **W119 fires the moment** an OFFICIAL
  executable per-problem test suite for ≥30 post-v6 functional problems appears on a clean
  official surface, OR a packaged `release_v7`+ / LCB-published construction provenance
  appears, OR a reachable stronger-than-Maverick model discloses a primary-KNOWN cutoff ≤
  the manifest frontier. **W119 satisfies the grader half directly by pivoting to the ICPC
  package family.**
* **W119** = a NEW sub-issue under `COO-6` (sibling of `COO-9`), created at milestone end
  with full results + a `COO-9` summary comment (the W105→W118 pattern).
  `linear_github_mapping.json` updated + `sync_linear_github_v1.py` validated.

---

## What is NOT in scope (anti-drift)

* **No** reopening MBPP+ V2 (W102 cap).
* **No** reopening the frozen cross-modal lines (RealWorldQA frozen at 11B).
* **No** reopening the closed Llama-3.1 rescue branch (W106 NO-GO).
* **No** APPS main-lane NIM (APPS stays the exposed control only).
* **No** 70B resistant reflexion de-noise (W109 rule).
* **No** dirty / contamination-EXPOSED / sample-only-graded benchmark sold as a frontier
  win.
* **No** raw-contest hand-assembled / aggregator-only / website-only / scraped instrument
  treated as admissible (the P1 official-package ∧ P5 no-operator-curation criteria REFUSE
  them).
* **No** operator-SYNTHESISED hidden tests (refused by P5/P8 — only the OFFICIAL shipped
  `data/secret` + shipped output validators count).
* **No** 405B expensive run unless reachability changes AND a pre-committed gate clears.
* **No** version bump, **no** PyPI publish, `coordpy/__init__.py` untouched.
* `ultracode` stays OFF (a bounded construction + certification milestone, not a repo-wide
  dynamic-workflow job). Threshold to reconsider: multiple official package families become
  live at once / a repo-wide package-certification migration / broad multi-surface external
  verification that cannot be done sequentially — none of which W119 required (the official
  ICPC org is one surface, swept sequentially). If crossed, say so explicitly before
  changing modes.

---

## Operational state (pre-W119 facts, held constant)

* **Two confirmed retirements STAND** — W89 (base HumanEval, +5.56 pp) + W105
  (HumanEval+, +7.00 pp), both `meta/llama-3.3-70b-instruct` @ 70B, contamination-EXPOSED
  HumanEval-family. W119 must not weaken these.
* **Resistant superiority = 0 clean across BOTH scales** (70B −3.33 / +0.00; Maverick
  +0.00; M3 sub-floor at 70B). REGISTERED ceiling (W114).
* Decision CID `258b6ed7` (W114=W115=W116=W117=W118). W119 must re-derive it
  byte-identically (the LCB-inherited verdict is reused unchanged).
* Reachability (W112 sweep, fixed prior — NOT re-probed): Maverick = reachable tier-1;
  Qwen3-Coder-480B / DeepSeek-V4-pro / Mistral-Small-4 = reachable tier-2;
  `meta/llama-3.1-405b-instruct` = 404×6. `NVIDIA_API_KEY` IS present in this run env.

---

## § 1 — α / β / γ branch logic (LOCKED)

* **Lane α (main, LIVE — official ICPC public-package construction):** pivot off the
  grader-less LCB family. Stay inside the OFFICIAL ICPC package family (`github.com/icpc`
  org / official ICPC Foundation archive surfaces), packages following the ICPC/Kattis
  Problem Package Format. Pre-commit the instrument rule (§ 3, P1..P8). Build a REAL
  constructor (discover official repos → enumerate package dirs → verify completeness →
  extract dates/validation-types/secret-data/accepted-solutions → deterministic
  inclusion/exclusion → machine-generated manifest → SHA pin + CID + date histogram). If
  the constructor yields ≥30 admitted post-cutoff resistant pass-fail problems WITH a
  self-test-verified grader (P7∧P8) AND § 4 certifies a model → build the slice, run the
  full real-data preflight, and earn the cheapest honest pilot (§ 6); else no-go (§ 7), but
  STILL land the constructor/manifest/admission machinery AND make the EXACT missing
  artifact machine-checkable.
* **Lane β (mandatory, LIVE — primary-source stronger-model cutoff attack):** re-check
  official cutoff disclosures for Maverick, Qwen3-Coder-480B, DeepSeek-V4-pro,
  Mistral-Small-4-2603, GLM-5, AND scan for any newly-reachable stronger model, from
  PRIMARY sources (official cards + release notes + official PDFs + vendor docs). Reuse the
  W118 disclosure matrix; integrate any newly-primary-KNOWN cutoff ≤ the manifest frontier
  immediately; else sharpen the blocker.
* **Lane γ (mandatory, NIM-free — package-to-pilot pipeline + graphify + truth):** land
  `coordpy.coordpy_icpc_public_functional_v1` (the constructor + the P1..P8 rule + the
  official ICPC source-family grader registry + the manifest validator + the reused C1..C4
  certification + the grader self-test + the slice gate + the W120 fire condition), reusing
  the W113 registry + W114 `certify_model_v1` + W117 `run_upstream_construction_v1` + the
  W116/W118 disclosure types (explicit-import-only, no duplication; the LCB-inherited
  decision CID re-derives byte-identically = `258b6ed7`). graphify refreshed start + close.
  **Land executable code/script assets, not just docs.**

---

## § 2 — official ICPC public-package family + provenance rule (LOCKED)

**Sources are the official ICPC organization repositories** (`github.com/icpc`) + official
ICPC Foundation archive surfaces — no random mirror, no third-party aggregator/scraper as
authority. Tooling: a real reproducible fetch (the GitHub API via `gh` for the LIVE
listing) + WebSearch/WebFetch against the official problem-package-format spec + model
cards/vendor docs/PDFs for Lane β (the documented W113–W118 convention; the chrome browser
MCP is reserved for `/browse` and is NOT used).

**The W119 LIVE official-ICPC-package pass (2026-05-30):**

| Repo | Created | Problems | secret `.in`/`.ans` | accepted refs | output validators | post-Aug-2024? |
|---|---|---|---|---|---|---|
| `icpc/na-rocky-mountain-2025-2026-public` | 2025-11-13 | 13 | 558 / 558 (REAL bytes) | 119 (py/cpp/java) | 8 | **YES (resistant)** |
| `icpc/na-rocky-mountain-2024-2025-public` | 2024-12-03 | 13 | 548 / 548 (REAL bytes) | 0 | 0 | **YES (resistant)** |
| `icpc/na-rocky-mountain-2023-2024-public` | 2024-01-16 | 13 | 708 | 0 | 0 | no (EXPOSED for Maverick) |

**Net (family-wide):** the official ICPC package family **SHIPS the executable functional
GRADER** (`data/secret/*.in`+`*.ans` + shipped output validators + accepted reference
solutions) — **the exact artifact W118 proved ABSENT in the LCB family.** LFS-vs-real check:
the secret files are **REAL TEST BYTES**, not Git-LFS pointers (verified by direct content
fetch: `airfaregrants/.../corner-1.in` = `1\n10\n`; `adriftatsea/.../001-N-N.in` = `N\nN`).
Grader self-test (NIM-free): the official accepted Python solutions for `videogames` (8/8)
and `whattimeisitmrfox` (8/8) reproduce the official `.ans` under a fresh-subprocess
token-normalized diff oracle = **16/16 cases PASS** ⇒ the grader is a REAL EXECUTABLE
ORACLE. **The W118 family-wide grader blocker is DISSOLVED.**

**Model cutoffs (Lane β, 2026-05-30, primary sources; reused W118 matrix):** Maverick KNOWN
"August 2024" (boundary 2024-08-31); Qwen3-Coder-480B / DeepSeek-V4-pro / Mistral-Small-4
v26.03 / GLM-5 all **NO CUTOFF STATED** (primary) ⇒ disclosure `{KNOWN: 1, UNKNOWN: 4}`;
**nothing newly primary-disclosed since W118.**

---

## § 3 — official-ICPC-package instrument rule (LOCKED — the W119 supply gate)

A constructed instrument is **IDENTITY-ADMISSIBLE** iff P1..P6 hold and it carries ≥
`MIN_RESISTANT_SLICE` = 30 admitted problems; **GRADER-ADMISSIBLE** iff P7 ∧ P8;
**PILOT-ADMISSIBLE** iff identity ∧ grader:

* **P1 official ICPC package source** — a repo of the official ICPC org (or ICPC
  Foundation archive), in the ICPC/Kattis Problem Package Format; NOT a mirror /
  aggregator / scraper.
* **P2 dated contest** — each problem carries an official contest date.
* **P3 post-cutoff resistant** — contest date STRICTLY AFTER the target model's KNOWN
  cutoff (Maverick Aug-2024 ⇒ `> 2024-08-31`).
* **P4 functional / code-generation-compatible** — a stdin→stdout programming problem the
  W89 mechanism can attack (interactive problems EXCLUDED).
* **P5 deterministic inclusion/exclusion (no operator curation)** — selection AND ordering
  are a total machine rule over the official package payload; NO hand-picking; NO
  operator-synthesised content.
* **P6 machine-generated manifest** — reproducible fetch + SHA-256 pin of the official
  package listing + content-addressed manifest CID + re-derivable date histogram.
* **P7 official executable GRADER PRESENT** — the package ships a per-problem hidden-test
  suite (`data/secret/*.in`+`*.ans`) gradeable by a clean diff oracle (pass-fail) or a
  SHIPPED output validator. **This is the artifact W118 proved absent — present here.**
* **P8 grader is verifiably EXECUTABLE** — an OFFICIAL accepted reference solution runs in
  a fresh isolated subprocess against the official secret cases and PASSES (the grader
  self-test). Sample-only / operator-synthesised graders refused; interactive excluded.

**On the W119 LIVE pass (§ 2):** P1..P8 ALL hold (grader present + self-test 16/16) — the
grader tier is SOLVED — but the post-cutoff resistant pass-fail count from the cleanest
single official surface = **24** (RMRC-2024-2025: 12 pass-fail, excl. 1 custom-without-
validator; RMRC-2025-2026: 11 pass-fail + 1 custom-with-validator, excl. 1 interactive) =
**24 < 30**. ⇒ the instrument is **GRADER-ADMISSIBLE but NOT IDENTITY-admissible on the
count** ⇒ NOT PILOT-admissible. The exact missing artifact: **+6 post-cutoff resistant
pass-fail problems with a shipped grader.**

---

## § 4 — per-model certification rule on the constructed manifest (LOCKED)

Reuse the W114 `certify_model_v1` C1..C4 gate (NO duplication) against the constructed
manifest's date histogram. C4 is instrument-specific (the ICPC manifest is a GENUINELY NEW
instrument none of the candidates has run, so `already_settled_on_instrument` resets to
False). A candidate is **PILOT-ADMISSIBLE** iff it is C1∧C2∧C3∧C4 identity-certifiable AND
the manifest is GRADER-admissible (P7∧P8) AND slice-admissible (≥30).

**On the W119 LIVE pass:** the 24-problem count is **load-bearing at BOTH levels** — it
blocks slice-admissibility AND the reused C2 gate (which requires ≥30 resistant problems
after the cutoff). So on the actual 24-problem slice **even Maverick is NOT
identity-certifiable** (C2: 24 < 30), and the three tier-2 models are C1-blocked (UNKNOWN
cutoff) ⇒ **0 certifiable models.** The W119 falsifiability test proves the count is the
SOLE gate: a synthetic ≥30 grader-clean slice DOES make Maverick C1∧C2∧C3∧C4-certifiable
AND pilot-admissible AND fires W120.

---

## § 5 — construction + slice + exclusion rule (LOCKED)

IF the constructor yields a GRADER-admissible manifest with ≥30 post-cutoff resistant
pass-fail problems AND § 4 certifies some candidate:

1. **Construct** the manifest from the official ICPC package listing (deterministic;
   machine-generated; SHA-pinned; CID-pinned; date histogram). DONE this milestone
   (24 problems; manifest CID `2b337377…`).
2. **Verify the grader** (P7) and self-test it (P8): run official accepted solutions vs
   official secret cases in a fresh subprocess. DONE (16/16 PASS).
3. `partition` the resistant slice for the target model's KNOWN boundary; EXCLUDE
   pre-cutoff / interactive / no-grader (typed breakdown). DONE.
4. Select the deterministic outcome-blind ≥30 slice; pin its CID. **BLOCKED** — only 24
   available (step 4 unreachable: 24 < 30).
5. Run the NIM-free preflight (manifest integrity + grader self-test + slice integrity);
   `pilot_earned` ⟺ all pass AND the slice has ≥30 problems WITH a working grader.

Step 4–5 are unreachable this milestone (count short at step 4).

---

## § 6 — pilot-earning rule (LOCKED)

A pilot is earned ONLY if BOTH are true:

1. **Lane α** yields a PILOT-admissible instrument (P1..P8 + ≥30 slice) certifiably
   resistant for at least one reachable model (§ 3 + § 4 + § 5); AND
2. **Lane β** yields a stronger-than-Maverick model with a primary-KNOWN cutoff that
   certifies on it AND is NOT redundant — OR Maverick on a GENUINELY NEW grader-clean
   instrument it never ran (a verdict-changing run: does the W113 Maverick resistant FAIL
   hold on an official ICPC-grader battlefield, or was it `release_v6`/LeetCode-form
   specific?).

Then the ONE earned expensive run is the cheapest honest Phase-2 pilot (1 seed × 30 × K=5
= 330 calls; a ≈22-call canary first), mechanism byte-identical to W89/W108/W113.

**On the W119 LIVE pass:** condition 1 fails (24 < 30 ⇒ NOT pilot-admissible; even Maverick
is C2-blocked by the count) ⇒ **NO pilot earned; $0 NIM.** `NVIDIA_API_KEY` is present, so
this is a clean count-gate no-go, not a reachability excuse. A pilot on a 24-task slice
would be a sub-threshold spend and is refused.

---

## § 7 — no-go rule if no pilot-admissible certifiable slice exists (LOCKED — the load-bearing branch)

If § 3 returns no PILOT-admissible instrument OR § 4 certifies no usable model, W119 STOPS
honestly with **$0 NIM** and records the blocker as a hard, dated, per-surface + per-model
spend gate (NOT surrender):

* **Instrument/grader side**: the GRADER tier is **SOLVED** (the official ICPC family ships
  real, self-test-passing executable graders — the W118 blocker DISSOLVED). The remaining
  blocker is the **SLICE COUNT**: only 24 post-cutoff resistant pass-fail problems from the
  cleanest single official surface (RMRC-2024-2025 + RMRC-2025-2026), 6 short of 30. The
  official NWERC-2024 static package zip 404s (no clean second-surface aggregation via the
  obvious path). The exact missing artifact is named (§ 3).
* **Model side**: every reachable stronger-than-Maverick model has a primary-UNDISCLOSED
  cutoff (C1). Maverick (Aug-2024 KNOWN) would be certifiable on a ≥30 grader-clean slice
  but is C2-blocked by the 24-count on the available slice.
* **Carry-forward** (re-affirmed, sharpened): the W114/W115/W116/W117/W118 caps STAND; W119
  ADDS the live official-ICPC-package grader DISSOLUTION (the grader exists + self-tests),
  the durable package-to-pilot pipeline, and the sharpened count blocker.
* This is **not** "give up" — it is the honest aggressive move: the blocker has MOVED from
  "abundant official identities, NO official grader" (W118) to "official grader EXISTS and
  SELF-TEST-PASSES on a genuinely-new resistant ICPC family; only +6 post-cutoff pass-fail
  tasks short of a clean pilot" (W119). The blocker is narrowed to ONE specific, named,
  machine-checkable artifact.

---

## § 8 — graphify deliverables (LOCKED — Lane γ)

* Refresh at start from HEAD (`graphify update .`; HEAD `0062de0`). The incremental
  updater correctly reported NO topology change between `32d3498`→`0062de0` (W118 sync was
  docs/mapping-only); the graph content already reflected HEAD's W118 code (verified:
  `coordpy_frontier_functional_v1` present 1144×). **DONE.**
* Use concretely: `explain run_livecodebench_reflexion_bench_v1 / partition_resistant_v1 /
  certify_model_v1 / run_frontier_functional_construction_v1`;
  `path run_w113_resistant_pilot.py run_livecodebench_reflexion_bench_v1`;
  `affected run_livecodebench_reflexion_bench_v1`. **DONE at start.**
* Refresh at end after all code/doc changes; confirm the graph is built from the W119 HEAD
  (the new W119 module is a real topology change forcing a header refresh);
  `explain run_icpc_public_construction_v1` shows the reuse edges to
  `run_upstream_construction_v1` + `certify_model_v1`.

---

## § 9 — W120 branch logic (LOCKED — pre-committed)

Selected by the W119 construction/certification verdict:

* **PILOT-ADMISSIBLE + CERTIFIABLE → pilot ran:** W120 is dictated by the pilot verdict via
  `interpret_cross_scale_resistant_result_v1`.
* **`NO_CERTIFIABLE_STRONGER_MODEL` (the expected branch):** the resistant-code superiority
  question is **SLICE-COUNT-BLOCKED** on the official-ICPC-grader battlefield (no longer
  grader-blocked). W120 fires the moment:
  - **(a) slice trigger** — the post-cutoff (>2024-08-31) resistant pass-fail count reaches
    `MIN_RESISTANT_SLICE = 30` on a clean official surface: the next official ICPC regional
    package drop (each RMRC adds ~12/yr), OR one clean official second-surface aggregation
    (an official NWERC/regional DOMjudge package). The grader is ALREADY present +
    self-test-passing, so a slice alone unlocks the cheapest honest verdict-changing
    Maverick pilot; OR
  - **(b) cutoff trigger** — a reachable stronger-than-Maverick model discloses a
    primary-KNOWN cutoff (widening the resistant set / strengthening the target); update
    `MODEL_TRAINING_CUTOFFS` + provenance, then re-run.
  Re-run `run_icpc_public_construction_v1` against the updated inputs → if a model becomes
  pilot-admissible, run the pre-committed cheapest-honest pilot on the strongest such
  target. Until one holds, the registered bounded ceiling STANDS and resistant-code NIM is
  BLOCKED on the missing +6 post-cutoff resistant pass-fail tasks. (A genuinely different
  non-code superiority axis may be selected instead; frozen/closed lines stay closed.)

In ALL branches: **W89 + W105 STAND**; `COO-9` stays lead unless the evidence forces a
different code-line move.

---

## § 10 — Stable boundary preservation (LOCKED)

* `coordpy.__version__ == "0.5.20"`; `coordpy.SDK_VERSION == "coordpy.sdk.v3.43"`; **no
  PyPI**; `coordpy/__init__.py` untouched.
* Advanced work explicit-import-only: 1 new module (`coordpy_icpc_public_functional_v1`) +
  1 script (`run_w119_icpc_public_construction_v1.py`); the module reuses the W113 registry
  + `normalize_contest_date_v1` + `MIN_RESISTANT_SLICE` + the W114 `certify_model_v1` /
  `LatestResistantInstrumentV1` / `STRONGER_MODEL_CANDIDATES` + the W117
  `run_upstream_construction_v1` + the W116 disclosure types + the W118 disclosure matrix
  (namespace import; no duplication). The LCB-inherited decision CID re-derives
  byte-identically (`258b6ed7`).
* 29th consecutive preflight/earn-discipline validation (W93–W119): the no-go branch is
  pre-committed by the rule, so the $0 spend is discipline, not omission. 18 new tests
  (incl. 2 falsifiability tests); 110 pass across W113–W119.

---

## Honest framing

W119 does **multiple** substantial things in one push: it makes the aggressive pivot W118
set up — off the grader-LESS LiveCodeBench source family onto the official ICPC
problem-package family that **already ships the executable grader** — and PROVES the pivot
works: a REAL constructor enumerates the official `github.com/icpc` org, verifies the
post-cutoff RMRC-2024-2025 + RMRC-2025-2026 packages ship **real (non-LFS) secret test
data + shipped output validators + accepted reference solutions**, and a NIM-free grader
self-test runs official accepted solutions against the official secret cases = **16/16
PASS** ⇒ the executable functional grader W118 proved ABSENT is now PRESENT and verified.
It attacks the model-disclosure side from PRIMARY sources (reused W118 matrix; nothing
newly disclosed). It SHIPS a durable package-to-pilot pipeline (the P1..P8 rule, the
official ICPC source-family grader registry, the deterministic manifest constructor + thin
live fetch, a real stdin/stdout executor, the reused C1..C4 + grader + slice gates, the
W120 fire condition) with two falsifiability tests. And it lands the honest verdict — **the
official ICPC grader EXISTS and self-test-PASSES on a genuinely-new resistant battlefield,
but the post-cutoff resistant pass-fail count from the cleanest single official surface is
24 < 30, which blocks BOTH slice-admissibility AND the C2 certification gate ⇒ no
pilot-admissible certifiable slice ⇒ $0 NIM.** The blocker has MOVED (sharper than W118):
from "abundant official identities, no official grader" to "official grader present +
self-test-passing, +6 post-cutoff resistant pass-fail tasks short of a clean pilot." If the
next official ICPC regional drop (or one clean official second-surface aggregation) adds 6
tasks, W119's pipeline makes the Maverick pilot push-button and loads W120 immediately;
since it does not yet, W119 makes the EXACT missing +6-task artifact load-bearing. A
sub-threshold 24-task pilot is not pilot-admissible and not a win; the bounded claim is the
registered truth floor, not surrender. `ultracode` stays OFF; `COO-9` stays lead.
