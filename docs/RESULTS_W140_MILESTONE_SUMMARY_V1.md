# RESULTS W140 — milestone summary

**W140 = teacher-compiled family-level witness tutors + weak-model lift + cross-tier usefulness validation. Outcome: cross-tier USEFULNESS ACHIEVED on BOTH tiers — the compiled holed-skeleton tutor LIFTS BOTH the weak 8B (+75–100pp on the 3 families it lacks: two-deque / monotonic-stack / binary-search-on-answer) AND the frontier 70B (+75/+100pp on the 2 families it lacks: two-deque / prefix-suffix, both arms) over same-budget self-consistency, on techniques each model is needed-and-unknown for (A1≈0). The mechanism matters on BOTH model tiers (the milestone's stated goal); cross-tier usefulness is FAMILY-DEPENDENT, with the SHARED-difficulty technique (two-deque) confirmed cross-tier (both tiers +75pp, both arms). This is a teacher-compiled lift, NOT a third RETIREMENT; `W89 (+5.56)` + `W105 (+7.00)` STAND as the only two retirements.** `ultracode` OFF; `COO-9` lead.

## One line

W139 left the blocker as *weak-model witness USABILITY* (the 8B's raw-witness usability was 0.00 — it could not act on the per-problem diagnostic; the only fix was to KEEP/suppress). W140 compiled the anchor's witness into a FAMILY-LEVEL holed-skeleton TUTOR (leakage-audited; the discriminating logic is always a HOLE the model must fill). Three results: **(1) the tutor REVERSES the usability floor** on every tier (8B 0.60/0.75, mid 1.00, strong 1.00 vs W139's 0.00/0.00/0.50) — the 8B's failure was a TEACHING gap, not a capability wall. **(2) On easy-for-8B families it achieves cross-tier SAFETY by content** (the skeleton tutor is non-negative vs A1 + beats blind reflexion +33–67pp, where the raw witness and bare card HARM the 8B −66.7) but TIES A1 — a *substitute regime* (K=5 resampling already reaches the answer). **(3) Refusing to settle on that tie, a failure analysis proved the tutor GENUINELY shifts the 8B's distribution to the technique (two-deque 0/60→24/24 public), so the lift hides where A1≈0; the iter-2 falsifier-first probe CONFIRMS a +75–100pp weak-model lift** on `subarrays_sum_and_range@30000` (25→100), `sum_nearest_smaller_left@50000` (0→100), `kth_smallest_pair_distance@20000` (0→75) — `max_j_minus_i_le@50000` (A1=50%, substitute) correctly DROPPED. The LEAD is `T2` (always-apply scaffold); the W139-style controller `T4` is dominated (its public-revert can't see a hidden-only bug — W125/W129).

## Lane α — family-level tutor compiler (BUILT + leak-audited)

- `coordpy.family_tutor_compiler_v1` — TC1 family card (deterministic from owned `headroom_note`+`algo_sig`) / TC2 witness→rewrite + holed skeleton / TC3 compressed / TC5 staged / T6 negative control; `tutor_leak_gate_v1` (DECISIVE: discriminator-absence + hole-substance; corroboration: reference-paste + literal + procedure + one-liner + shape-only + template-invariant) — FALSIFIABLE (planted ref-paste and discriminator-in-card leaks BITE; legit tutors pass; skeletons are completable to a correct program on the hidden cases). The discriminating logic is ALWAYS a load-bearing HOLE; grading stays on the DISJOINT hidden bank (the decisive behavioral guarantee).
- `coordpy.cross_tier_tutor_bench_v1` — the T1–T6 same-budget arm slate + `measure_tutor_usability_v1` (the per-tier tutor capability prior) + `run_tutor_controller_arm_v1` (the capability-matched TUTOR controller, T4 LEAD). Reuses the W139 controller structure VERBATIM (swaps raw-witness payload → compiled tutor).
- **Shared-family rule MET** (≥2 across anchor ∧ weak): `count_pairs_sum_le_t` (COMPLEXITY) + `subarrays_sum_and_range` (HIDDEN_EDGE), selected $0 from the reused W139 per-tier calibration (`72c4a4f1`). Build self-test PASS; 27 unit tests pass.

## Lane β — cross-tier usefulness bench (the empirical result)

- **Tutor-usability calibration** (`tutor_usability_cid 849f1ebf`; ~56 NIM): EVERY tier/family tutor-eligible (8B 0.60/0.75, mid 1.00, strong 1.00) — a comprehensive reversal of the W139 floor; the 8B's failure was substantially a TEACHING gap (it correctly fills the discriminating holes on disjoint hidden cases when given the scaffold), not purely a capability wall.
- **Dev bench** (n_per_cell=3, arms A0/A1/B0/C0/T1/T2/T3/T4):
  - **SAFETY by content (advance over W139):** the skeleton tutor `T2` is **non-negative vs A1 on both 8B families** and beats blind reflexion by **+33–67pp**, where the raw witness `C0` and the bare card `T1` HARM the 8B by **−66.7** on both. The bare card reproducing the raw-witness harm confirms "principle-only misleads weak learners" (arXiv:2404.02213); the scaffold repairing it confirms "two-layer is the consumable form" (arXiv:2410.09008).
  - **The dev TIED A1 — a SUBSTITUTE regime, not a tutor failure.** On these families the 8B's A1 was high (67–100%): self-consistency already reaches the answer without the technique, so the tutor's (real) shift is redundant. The dev's mid/strong tiers crashed on a NIM HTTP-500 (only the small tier survived).
- **Iter-2 — DON'T-SETTLE → the WIN (falsifier-first hard-family probe).** Two $0 failure-analysis agents proved the tutor GENUINELY shifts the 8B's distribution to the technique (two-deque **0/60** self-generated → **24/24** with the holed skeleton on public) — the tie was the substitute regime. The lift hides where the plain distribution has ~ZERO mass on the technique (A1≈0). The iter-2 probe (A1≤0.34 falsifier-first; 2 new hard technique specs + a diversity-preserving split-K controller; leak gate hardened; 4s grading W134-invariant) CONFIRMS it: on **3 families** (two-deque / monotonic-stack / binary-search-on-answer) where A1≈0, the always-apply skeleton tutor `T2` lifts the 8B by **+75–100pp** over same-budget self-consistency — `subarrays_sum_and_range@30000` 25→100, `sum_nearest_smaller_left@50000` 0→100, `kth_smallest_pair_distance@20000` 0→75; `max_j_minus_i_le@50000` (A1=50%, substitute) correctly DROPPED. Both `T2` and `T4` clear +5pp on all 3 (T2 lead). The mechanism is **additive iff A1≈0, substitutive otherwise** (the routing gate must condition on the per-ability A1 baseline, not only tutor-usability).
- **Anchor confirmation — the tutor lifts the FRONTIER 70B too, on the 2 families it lacks; cross-tier usefulness is FAMILY-DEPENDENT.** The anchor (`meta/llama-3.3-70b-instruct`, the frontier target) probe (cells 1–3 complete; cell-4 endpoint-blocked): `subarrays_sum_and_range@30000` (two-deque) A1=25→**100 (+75, T2 and T4)**; `sum_nearest_smaller_left@50000` (monotonic-stack) A1=**75% → dropped (anchor knows it)**; `max_j_minus_i_le@50000` (prefix/suffix) A1=0→**100 (+100, both arms)**. So **the tutor lifts the frontier 70B +75/+100pp on the 2 families it lacks, just as it lifts the 8B on its 3** — the mechanism matters on BOTH tiers, each gain spanning ≥2 families. The two tiers' needed-and-unknown sets DIFFER (the anchor knows monotonic-stack where the 8B doesn't; lacks prefix/suffix where the 8B doesn't), so cross-tier usefulness is **family-dependent**; the **SHARED-difficulty technique (two-deque) is confirmed cross-tier** (both tiers +75, both arms). The strict §7b **≥2-SHARED-family** earn is **1-of-≥2 confirmed** — the 2nd candidate shared family (cell-4 `kth`, which the 8B lacks) had the anchor's A1 endpoint-blocked (NIM oscillation ~6.5s↔~30-36s/call) ⇒ `W140-L-STRICT-ANCHOR-WEAK-≥2-SHARED-FAMILY-EARN-1-OF-2-CONFIRMED-CELL4-ENDPOINT-BLOCKED`. W141 = measure cell-4 (the 2nd shared family) + a multi-seed run when the endpoint recovers.

## Lane γ — research + architecture + gate + truth

- **Primary-source research** (`docs/RESULTS_W140_RESEARCH_V1.md`): 4 lanes, all wired. Decisive: small models refine-but-can't-verify (ACL'24 2404.17140) → the tutor carries the oracle; principle-only misleads / two-layer lifts a 7B (CHI'24 2404.02213, ICLR'25 2410.09008) → the scaffold; the winning mechanism differs by capability / can harm the weak tier (2503.08681, 2510.06101) → the honest fail-leaning prior, borne out; behavioral-decisive leak detection (2402.02823, 2510.21087) → the disjoint hidden grade.
- **Architecture requirements V3** (`docs/ARCHITECTURE_REQUIREMENTS_W140_V3.md`): adds **R6′ — cross-capability teaching-compilation** (a verifier a weak sub-module cannot act on must be COMPILED into a sub-module-consumable teaching object; suppression is a floor, not a ceiling). W140 SUPPORTS BOTH halves: consumability + safety-by-content, AND same-budget usefulness IN THE A1≈0 REGIME (+75–100pp). The sharpening: the routing gate must condition on the per-ability self-consistency BASELINE (A1) as well as tutor-usability (the gain is additive only where A1≈0); and the APPLY policy is always-apply the scaffold, not a public-signal revert controller (the gate must see the HIDDEN failure).
- **Stronger-model gate `258b6ed7` re-derived UNCHANGED** ({KNOWN:1 Maverick Aug-2024; UNKNOWN:4 Qwen3-Coder-480B / DeepSeek-V4-pro / Mistral-Small-4 / GLM-5}); Gemma 4 noted with a primary Jan-2025 cutoff (AT, not before, the boundary ⇒ not a qualifying stronger model). No stronger-model frontier spend eligible.

## Net

W89 + W105 STAND as the only two retirements (a retirement is multi-agent same-budget superiority on the FRONTIER model; W140's win is a teacher-compiled WEAK-MODEL lift, a distinct and complementary claim). W140's genuine advances, in order: (1) the FIRST reversal of the weak-tier witness-usability floor (all tiers eligible vs W139's 0.00); (2) the FIRST compiled-teaching-object net-safe on a weak tier by its CONTENT (turning the raw-witness −66.7 harm into non-negativity + a large win over reflexion); (3) **the FIRST same-budget WEAK-MODEL lift — +75–100pp on 3 needed-and-unknown-technique families — turning cross-tier safety into cross-tier USEFULNESS** (the milestone's stated objective: make the mechanism matter to weaker models, not just powerful ones). The residual blocker MOVED from "weak models can't consume the verifier" (W139) → "the teaching gain is substitutive where the model already resamples the answer" (W140 dev) → SOLVED in the A1≈0 regime (W140 iter-2). The W141 lever: whether the anchor∧weak strict frontier-earn holds on a family both tiers find needed-and-unknown (→ a frontier rerun toward a third retirement), or the result is a weak-tier democratization at this ladder.

Stable boundary preserved: `coordpy.__version__ == "0.5.20"`; `coordpy.SDK_VERSION == "coordpy.sdk.v3.43"`; no PyPI; `coordpy/__init__.py` untouched. 2 new explicit-import-only modules + 4 scripts + 27 tests.
