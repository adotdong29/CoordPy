# Theorem registry — canonical status

> Canonical, single-source-of-truth registry for every named
> theorem-style claim in the Context Zero / Wevra programme.
> If this file disagrees with any other doc on the *status* of a
> claim, this file is right and the other file is stale.
>
> Status vocabulary (definitions in `docs/HOW_NOT_TO_OVERSTATE.md`):
>
> - **proved** — mathematical proof or proof-by-inspection.
> - **proved-conditional** — proof depends on a stated assumption.
> - **mechanically-checked** — runtime audit on every run; soundness by inspection.
> - **empirical** — measured on a published bench from a published seed.
> - **conjectural** — stated, falsifiable; not yet proved or systematically tested.
> - **retracted** — earlier reading withdrawn; replaced by a more honest reading.
>
> Last touched: SDK v3.19, 2026-04-28.

## Capsule algebra (W3-7 .. W3-16)

| Claim   | One-line description                                                  | Status                | Code/proof anchor                                                                 |
| ------- | --------------------------------------------------------------------- | --------------------- | --------------------------------------------------------------------------------- |
| W3-7    | CID is parent-permutation invariant                                    | proved                | `_capsule_cid` sorts parents; test `test_c1_parent_order_is_canonicalised`        |
| W3-8    | Budget monotonicity of admissibility                                   | proved                | `CapsuleLedger.admit`; `_capsule_cid` byte check                                  |
| W3-9    | Ledger DAG is acyclic; append order is topo                            | proved                | `CapsuleLedger.admit` parental check; `ancestors_of` BFS terminates               |
| W3-10   | Chain tamper-evidence under SHA-256 second-preimage                    | proved-conditional    | `_chain_step`, `verify_chain`; `test_c5_hash_chain_detects_tamper`                |
| W3-11   | Capsule subsumption — partial (4 cases)                                | proved (partial)      | adapters in `vision_mvp/wevra/capsule.py`                                          |
| W3-12   | Capsule view is a faithful header projection                            | proved                | `as_header_dict`, `render_view`                                                  |
| W3-13   | DAG height ≤ 4 on canonical run pattern                                  | proved                | inspection of `build_report_ledger`                                              |
| W3-14   | Per-capsule budget locality — negative                                  | proved (negative)     | `test_phase47_cohort_subsumption.py::test_per_capsule_budget_cannot_bound_total_count` |
| W3-15   | Cohort lift                                                              | proved                | `capsule_from_cohort`; cohort tests                                              |
| W3-16   | Relational limitation post-cohort-lifting — negative                    | proved (negative)     | cohort relational tests                                                          |

## Decoder frontier (W3-17 .. W3-31)

| Claim   | Description                                                                | Status                | Anchor                                                                                                  |
| ------- | -------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------- |
| W3-17   | Admission-only rules ≤ priority-decoder ceiling under ceiling-forcing      | proved-conditional    | `phase47_bundle_learning.py`                                                                            |
| W3-18   | Plurality > priority on coherent-majority bundles                          | proved-conditional    | `test_phase48_bundle_decoding.py`                                                                       |
| W3-19   | Learned bundle decoder breaks 0.200 ceiling at +15pp on $n=80$             | empirical             | `phase48_bundle_decoding.py` reproducible from default seeds                                            |
| W3-20   | Deep Sets sufficiency (capacity statement)                                 | proved                | `capsule_decoder_v2.py::_phi_capsule`                                                                   |
| W3-21   | Linear-class sign-flip asymmetry — negative                                | proved                | `phase49_symmetric_transfer.py`                                                                         |
| W3-22   | Pooled-multitask symmetric transfer at $n=80$                              | empirical             | Phase 49 result (0.350 / 0.350)                                                                         |
| W3-23   | DeepSet best-cell 0.425 at $n=80$                                          | empirical             | Phase 49 result                                                                                         |
| W3-24   | Post-search winner's-curse bias                                            | proved                | Phase 50                                                                                                 |
| W3-25   | Tail-replication / hold-out generalisation gap                             | proved-conditional    | Phase 50                                                                                                |
| W3-26   | DeepSet best-cell 0.362 at $n=320$                                         | empirical             | Phase 50 — falsifies W3-C7 strict reading                                                               |
| W3-27   | 6-family zero-shot max-penalty +0.112 at $n=320$                           | empirical             | Phase 50 — falsifies W3-C7 penalty reading                                                              |
| W3-28   | Sign-stable DeepSet zero-shot gap = 0.000 at level 0.237                   | empirical             | Phase 50 — direction-invariance, not level-matching                                                     |
| W3-29   | Bayes-divergence zero-shot risk lower bound on linear family               | proved                | Phase 50 structural argument                                                                            |
| W3-30   | Strict separation: relational decoder vs. magnitude-monoid                 | proved                | Phase 51                                                                                                |
| W3-31   | Empirical relational-decoder level-lift                                    | empirical             | Phase 51                                                                                                |

## Capsule-native execution (W3-32 .. W3-41)

| Claim                  | Description                                                                  | Status                            | Anchor                                                                          |
| ---------------------- | ---------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------- |
| W3-32                  | Lifecycle ↔ execution-state correspondence (spine kinds)                     | proved                            | `capsule_runtime.py`; `test_wevra_capsule_native.py`                            |
| W3-32-extended         | W3-32 carry-over to PATCH_PROPOSAL / TEST_VERDICT                            | proved                            | `test_wevra_capsule_native_intra_cell.py`                                       |
| W3-33                  | Content addressing at artifact creation time                                  | proved                            | `seal_and_write_artifact`; cross-validation test                               |
| W3-34                  | In-flight ↔ post-hoc CID equivalence on non-ARTIFACT spine kinds              | proved                            | `test_w3_34_spine_equivalence_holds`                                            |
| W3-35                  | Parent-CID gating is the execution contract                                   | proved                            | runtime gate raises `CapsuleLifecycleError`                                     |
| W3-36                  | Meta-artifact circularity is sharp; detached-witness corollary                | proved (negative + constructive)  | `test_meta_manifest_seals_in_secondary_ledger`                                  |
| W3-37                  | Chain-from-headers verification                                                 | proved                            | `verify_chain_from_view_dict`; tamper tests                                     |
| W3-38                  | ARTIFACT audit-time on-disk re-hash                                            | proved                            | `verify_artifacts_on_disk`                                                       |
| **W3-39 (SDK v3.3)**   | **PARSE_OUTCOME lifecycle gate + parse → patch → verdict DAG chain**          | **proved**                        | `test_wevra_capsule_native_deeper.py::ParseOutcomeLifecycleTests`               |
| **W3-40 (SDK v3.3)**   | **Lifecycle-audit soundness on L-1..L-8**                                     | **proved + mechanically-checked** | `lifecycle_audit.py`; `LifecycleAuditTests`                                     |
| **W3-41 (SDK v3.3)**   | **Deterministic-mode CID determinism on full DAG**                            | **proved + empirical**            | `DeterministicModeTests::test_w3_41_two_runs_collapse_to_identical_cids`        |
| **W3-42 (SDK v3.4)**   | **PROMPT lifecycle gate (parent = SWEEP_SPEC; idempotent on content)**         | **proved**                        | `test_wevra_capsule_native_inner_loop.py::PromptCapsuleLifecycleTests`         |
| **W3-43 (SDK v3.4)**   | **Prompt → response parent gate (LLM_RESPONSE has 1 parent = sealed PROMPT)**  | **proved**                        | `test_wevra_capsule_native_inner_loop.py::LLMResponseCapsuleLifecycleTests`     |
| **W3-44 (SDK v3.4)**   | **PARSE_OUTCOME → LLM_RESPONSE chain coordinate consistency**                  | **proved + mechanically-checked** | `lifecycle_audit.py::_check_l11`; `LifecycleAuditExtendedTests`                |
| **W3-45 (SDK v3.4)**   | **Lifecycle-audit soundness extends to L-1..L-11**                             | **proved + mechanically-checked** | `lifecycle_audit.py`; `LifecycleAuditExtendedTests::test_full_chain_audit_is_ok` |

## Team-level capsule coordination (W4-1 .. W4-3) — SDK v3.5

| Claim   | Description                                                                  | Status                            | Anchor                                                                          |
| ------- | ---------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------- |
| **W4-1 (SDK v3.5)** | **Team-lifecycle audit soundness on T-1..T-7**                                | **proved + mechanically-checked** | `team_coord.py::audit_team_lifecycle`; `TeamLifecycleAuditTests`                |
| **W4-2 (SDK v3.5)** | **Coverage-implies-correctness on the deterministic team decoder**            | **proved-conditional**            | `team_coord.py::TeamCoordinator`; `TeamLevelCorrectnessTests::test_w4_2_*`     |
| **W4-3 (SDK v3.5)** | **Local-view limitation: per-role budget below causal-share floor cannot be rescued by any admission policy** | **proved-negative**               | `TeamLevelCorrectnessTests::test_w4_3_*`; `phase52_team_coord.run_phase52_budget_sweep` |

## Two-Mac distributed inference + real cross-LLM (W5-1 .. W5-3) — SDK v3.6

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W5-1 (SDK v3.6)**  | **Real cross-LLM parser-boundary saturation: ``qwen3.5:35b`` (36B-MoE Q4) under strict parsing yields ``failure_kind=unclosed_new`` 10/10; ``qwen2.5:14b-32k`` yields ``ok`` 10/10; cross-model TVD on strict = 1.000; robust mode collapses TVD to 0.000** | **proved-empirical (real LLM)**    | `parser_boundary_real_llm.py`; result JSON `/tmp/wevra-distributed/real_cross_model_n10.json`         |
| **W5-2 (SDK v3.6)**  | **Backend integration: ``run_sweep(..., llm_backend=<duck-typed>)`` routes inner-loop calls through the backend; PROMPT/LLM_RESPONSE/PARSE_OUTCOME/PATCH_PROPOSAL/TEST_VERDICT chain seals byte-for-byte equivalently regardless of backend** | **proved**                        | `test_wevra_llm_backend.py::RunSweepBackendIntegrationTests`                                          |
| **W5-3 (SDK v3.6)**  | **``MLXDistributedBackend`` wire shape: OpenAI-compatible POST /v1/chat/completions with ``{model, messages, max_tokens, temperature, stream:false}``; parses ``choices[0].message.content``** | **proved**                        | `test_wevra_llm_backend.py::MLXDistributedBackendWireShapeTests`                                      |

## Model-scale vs capsule-structure on multi-agent coordination (W6-1 .. W6-4) — SDK v3.7

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W6-1 (SDK v3.7)**  | **Capsule-team lifecycle audit T-1..T-7 holds for every (regime × strategy × scenario) cell of the Phase-53 scale-vs-structure benchmark when driven by a real-LLM producer-role extractor (3 regimes: synthetic / qwen2.5:14b-32k / qwen3.5:35b × 4 capsule strategies × 5 scenarios = 60/60).** | **proved + mechanically-checked** | `phase53_scale_vs_structure.py::audit_ok_grid`; `docs/data/phase53_scale_vs_structure_K4_n5.json`; `test_wevra_scale_vs_structure.py::Phase53AuditOkGridTests` |
| **W6-2 (SDK v3.7)**  | **Phase-53 driver accepts any duck-typed ``LLMBackend`` substitute as the producer-role extractor backend; the team-coord pipeline seals TEAM_HANDOFF / ROLE_VIEW / TEAM_DECISION capsules end-to-end against an arbitrary backend, with no spine modification.** | **proved**                        | `test_wevra_scale_vs_structure.py::LLMExtractorBackendDuckTypingTests`                                |
| **W6-3 (SDK v3.7)**  | **``parse_role_response`` is robust on the closed-vocabulary claim grammar: accepts ``KIND <sep> payload`` for ``sep ∈ {|, :, -, –, —}``, rejects kinds outside the allowed list, deduplicates by kind (first wins), strips preamble noise, and treats ``NONE`` as zero claims (skip-not-early-return semantics).** | **proved + mechanically-checked** | `test_wevra_scale_vs_structure.py::ParseRoleResponseRobustnessTests` (16 cases)                       |
| **W6-4 (SDK v3.7)**  | **Phase-53 default config (K_auditor=4, T_auditor=128, n_eval=5, prompt-style identical, ``temperature=0``) yields ``accuracy_full = 0.800`` for substrate / capsule_fifo / capsule_priority / capsule_coverage in every model regime. ``capsule_learned`` (trained OOD on Phase-52 synthetic+noise) yields 0.400 on synthetic / 14B and 0.800 on 35B. ``structure_gain[regime]`` is non-positive at every regime (-0.4, -0.4, 0.0); ``scale_gain[capsule_learned] = +0.4``; ``scale_gain[fixed] = 0.0``; ``delta_with_scale = +0.4``. Cross-model candidate-kind TVD (14B vs 35B) = 0.167.** | **proved-empirical (real LLM, n=5 saturated)** | `docs/data/phase53_scale_vs_structure_K4_n5.json`; `docs/RESULTS_WEVRA_SCALE_VS_STRUCTURE.md`         |

## Cross-role cohort-coherence multi-agent coordination (W7-1 .. W7-3) — SDK v3.8

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W7-1 (SDK v3.8)**  | **FIFO unbeatability under low surplus: when the producer's emission stream satisfies ``|candidates(scenario)| ≤ K_role`` for every scenario in the bench, every fixed admission policy (FIFO, claim_priority, coverage_guided, cohort_coherence, learned) is permutation-equivalent to ``admit_all``; ``structure_gain`` over substrate FIFO is identically zero by construction.** Phase-53 default config (``mean_n_admitted_auditor < K_auditor=4`` in every regime) is the empirical anchor in the *positive* direction. | **proved-empirical**              | `docs/data/phase53_scale_vs_structure_K4_n5.json`; `phase53_scale_vs_structure.py`                    |
| **W7-1-aux (SDK v3.8)** | **Streaming cohort-coherence admission is unstable under candidate-arrival permutation: under a stream where the first auditor-routed candidate carries a foreign-service tag, ``CohortCoherenceAdmissionPolicy(fixed_plurality_tag=None)`` admits the foreign tag and rejects every subsequent gold-tag candidate, producing ``mean_n_admitted_auditor`` strictly less than the gold cohort size.** On Phase-54 default, streaming cohort ties FIFO at ``accuracy_full = 0.000`` across 10/10 scenarios. The buffered variant (W7-2 anchor) corrects this by pre-fitting the plurality off-line. | **proved-empirical (n=10 saturated)** | `test_wevra_cross_role_coherence.CohortPolicyUnitTests::test_streaming_locks_to_first_admitted_tag`; `Phase54DefaultConfigTests::test_streaming_cohort_does_not_beat_fifo_at_K4` |
| **W7-2 (SDK v3.8)**  | **Cross-role cohort-coherence structural win under gold-plurality: when the candidate stream satisfies the gold-plurality property (gold service tag has strictly more auditor-routed candidates than any decoy service tag) AND ``|candidates| > K_auditor``, the buffered ``CohortCoherenceAdmissionPolicy`` (constructed via ``from_candidate_payloads``) achieves ``accuracy_full = 1.000`` while substrate FIFO, ``CapsuleFifoAdmissionPolicy``, ``ClaimPriorityAdmissionPolicy``, ``CoverageGuidedAdmissionPolicy``, and the streaming cohort variant all produce ``accuracy_full = 0.000``. Headline gap = +1.000 on the pre-committed Phase-54 default (K_auditor=4, T_auditor=128, n_eval=10), stable across 5/5 bank seeds (11, 17, 23, 29, 31).** | **proved-empirical (n=50 saturated)** | `docs/data/phase54_cross_role_coherence_K4_n10.json`; `Phase54DefaultConfigTests::test_buffered_cohort_strictly_beats_fifo_at_K4`; `Phase54DefaultConfigTests::test_result_stable_across_bank_seeds` |
| **W7-2-conditional (SDK v3.8)** | **W7-2 holds iff K_auditor is in the structure-win window: ``gold_count_min ≤ K_auditor < min(K_auditor+1, total_candidates)``. The Phase-54 budget sweep observes ``+0.400`` gap at K∈{2,3} (gold cohort partially fits) and ``+1.000`` gap at K∈{4,5,6,8} (gold cohort fully fits; foreign-service decoys filtered out by cohort regardless of K). Sharper observation: the decoder's ``services_correct`` set-equality check makes service-blind admission unable to ever achieve services_correct on foreign-service-decoy benches, even with no budget pressure.** | **proved-empirical (K-sweep, K∈{2,3,4,5,6,8})** | `docs/data/phase54_cross_role_coherence_budget_sweep.json` |
| **W7-3 (SDK v3.8)**  | **Extraction floor (proved-negative): no per-role admission policy (FIFO, priority, coverage, learned, streaming or buffered cohort coherence) can recover a missing causal claim that the producer never emitted into the candidate stream. Proof: ``capsule_role_view.parents`` is constructed from ledger CIDs (Capsule Contract C5); a never-emitted claim has no CID; therefore no ROLE_VIEW can include it.** Empirical anchor: Phase-53 ``deadlock_pool_exhaustion`` 1/5 miss across all 5 admission strategies in every model regime; ``DEADLOCK_SUSPECTED`` is never emitted by 14B / 35B from role-local events. Implication: separates admission-fixable failures from extraction-fixable failures; the producer's claim coverage is a strict upper bound on every downstream admission strategy. | **proved-negative**               | Capsule Contract C5; `docs/data/phase53_scale_vs_structure_K4_n5.json` (failure_hist on `deadlock_pool_exhaustion`) |

## Cross-role corroboration multi-agent coordination (W8-1 .. W8-4) — SDK v3.9

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W8-1 (SDK v3.9)**  | **Cross-role corroboration strict separation: when the candidate stream satisfies the *decoy-plurality + cross-role-corroborated-gold* property (some decoy service has strictly more raw mentions than gold AND the gold service is mentioned by strictly more distinct producer roles than any decoy) AND ``|candidates| > K_auditor``, the buffered ``CrossRoleCorroborationAdmissionPolicy`` (constructed via ``from_candidate_stream``) achieves ``accuracy_full = 1.000`` while substrate FIFO, ``CapsuleFifoAdmissionPolicy``, ``ClaimPriorityAdmissionPolicy``, ``CoverageGuidedAdmissionPolicy``, AND the SDK v3.8 buffered ``CohortCoherenceAdmissionPolicy`` (W7-2) all produce ``accuracy_full = 0.000``. Headline gap = +1.000 on the pre-committed Phase-55 default (K_auditor=4, T_auditor=128, n_eval=10), stable across 5/5 bank seeds (11, 17, 23, 29, 31). The **first strict separation between cross-role corroboration and single-tag plurality cohort** in the programme.** | **proved-empirical (n=50 saturated)** | `docs/data/phase55_decoy_plurality_K4_n10.json`; `docs/data/phase55_seed_sweep.json`; `Phase55DefaultConfigTests::test_corroboration_strictly_beats_W7_2_at_K4`; `Phase55SeedStabilityTests::test_gap_holds_across_five_seeds` |
| **W8-2 (SDK v3.9)**  | **Score-function strict-ordering theorem (proved): for the corroboration score ``score(tag) = W_role · |distinct_roles(tag)| + |raw_mentions(tag)|``, if ``W_role > Δr_max`` (max raw-count difference between any two tags in the stream), then for any pair (t1, t2) with ``|distinct_roles(t1)| > |distinct_roles(t2)|`` we have ``score(t1) > score(t2)`` regardless of raw-count distribution.** Proof by inspection of the score function. Practical consequence: with default ``W_role=100``, no candidate stream of size < 100 can produce a raw-count-only override of a 1-role corroboration advantage. | **proved**                        | `team_coord.CrossRoleCorroborationAdmissionPolicy` docstring; `CorroborationPolicyUnitTests::test_w8_2_role_weight_dominates_raw_count` |
| **W8-3 (SDK v3.9)**  | **Backward-compat: corroboration matches W7-2 buffered cohort on the Phase-54 default (gold-plurality + foreign-decoys). Both policies achieve ``accuracy_full = 1.000``; corroboration is a strict generalisation of W7-2.** | **proved-empirical (n=10)**       | `docs/data/phase55_cross_regime.json` `phase54_default` entry; `CrossRegimeBackwardCompatibilityTests::test_corroboration_matches_W7_2_on_phase54` |
| **W8-4 (SDK v3.9)**  | **Decoy-corroboration falsifier (proved-empirical): when the decoy service has strictly more distinct producer roles than gold (``build_phase55_falsifier_bank``), buffered corroboration picks the decoy plurality and ties FIFO at ``accuracy_full = 0.000`` on 10/10 falsifier scenarios. The W8-1 win does NOT hold in this regime — by construction. Sharper observation: even at K_auditor=8 (no budget pressure), the W8-1 conditional regime requires the corroboration property; without it, the decoder's set-equality ``services_correct`` check is unrescuable by any service-blind admission policy.** | **proved-empirical (n=10 falsifier saturated)** | `docs/data/phase55_falsifier_K4_n10.json`; `Phase55FalsifierTests::test_corroboration_ties_fifo_on_falsifier`; `docs/data/phase55_budget_sweep.json` |

## Multi-service cross-role corroboration (W9-1 .. W9-4) — SDK v3.10

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W9-1 (SDK v3.10)** | **Multi-service corroboration strict separation: when the candidate stream satisfies the *multi-service-gold-corroborated* property (every gold service is mentioned by ≥ 2 distinct producer roles AND every non-gold service is mentioned by ≤ 1 distinct producer role) AND ``|candidates| > K_auditor``, the buffered ``MultiServiceCorroborationAdmissionPolicy`` (``top_k=2, min_corroborated_roles=2``) achieves ``accuracy_full = 1.000`` while substrate FIFO, ``CapsuleFifoAdmissionPolicy``, ``ClaimPriorityAdmissionPolicy``, ``CoverageGuidedAdmissionPolicy``, the buffered ``CohortCoherenceAdmissionPolicy`` (W7-2), AND the buffered ``CrossRoleCorroborationAdmissionPolicy`` (W8) all produce ``accuracy_full = 0.000``. Headline gap = +1.000 on the pre-committed Phase-56 default (K_auditor=4, T_auditor=128, n_eval=10), stable across 5/5 bank seeds (11, 17, 23, 29, 31). The **first strict separation between multi-service top-K corroboration and single-tag corroboration** in the programme.** | **proved-empirical (n=50 saturated)** | `docs/data/phase56_multi_service_K4_n10.json`; `docs/data/phase56_seed_sweep.json`; `Phase56DefaultConfigTests::test_multi_service_strictly_beats_w8_at_K4`; `Phase56SeedStabilityTests::test_gap_holds_across_five_seeds` |
| **W9-2 (SDK v3.10)** | **Argmax-by-role-count strict-ordering theorem (proved): for ``_dominant_tag_set`` with ``min_corroborated_roles ≥ 1``: (a) any tag with ``|distinct_roles| < min_corroborated_roles`` is excluded; (b) if ``|argmax_roles(stream)| = 1``, the dominant set has size 1 (collapses to W8); (c) if ``|argmax_roles(stream)| = k₀ ≤ top_k``, the dominant set equals the argmax tier (size ``k₀``).** Proof by inspection of ``_dominant_tag_set``. Practical consequence: with default ``min_corroborated_roles=2``, no single-role decoy can ever enter the dominant set; with default ``top_k=2``, at most two tags are admitted. | **proved**                        | `team_coord.MultiServiceCorroborationAdmissionPolicy` docstring; `_dominant_tag_set`; `MultiServicePolicyUnitTests::test_dominant_set_*` |
| **W9-3 (SDK v3.10)** | **Backward-compat with W8 + W7-2: on the Phase-55 default (single-service-gold; gold has strictly more distinct producer roles than every decoy), W9 admits exactly the same set as W8 by W9-2(b), and both achieve ``accuracy_full = 1.000``. Similarly, on Phase-54 default, W9 ties W7-2 at 1.000. W9 is a strict generalisation of both.** | **proved-empirical (n=10)**       | `docs/data/phase56_cross_regime.json` `phase55_default` and `phase54_default` entries; `W9BackwardCompatTests::test_w9_matches_w8_on_phase55_default`; `W9BackwardCompatTests::test_w9_admits_same_set_as_w8_when_one_tag_dominates` |
| **W9-4 (SDK v3.10)** | **Decoy-corroboration falsifier (proved-empirical): when at least one decoy service is corroborated by ≥ ``min_corroborated_roles`` distinct producer roles AND its corroboration score is competitive with at least one gold service's, the W9 dominant set includes the decoy (by W9-2(c) the argmax tier expands to include it) and ``services_correct`` fails on 10/10 falsifier scenarios. The W9-1 win does NOT hold in this regime — by construction. Sharper observation: this is the **structural limit of any service-blind admission policy** under multi-service decoder set-equality grading; the only escape is a bundle-aware decoder companion (W9-C1).** | **proved-empirical (n=10 falsifier saturated)** | `docs/data/phase56_falsifier_K4_n10.json`; `Phase56FalsifierTests::test_multi_service_ties_fifo_on_falsifier` |

## Bundle-aware team decoder (W10-Λ, W10-1..W10-4) — SDK v3.11

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W10-Λ (SDK v3.11)** | **Service-blind admission structural limit on R-57: on the Phase-57 default bank (multi-service-gold + corroborated-decoy via non-causal claim_kinds), every admission policy in the SDK whose state is a function of the (role, tag) bipartite multiset alone — `FifoAdmissionPolicy`, `ClaimPriorityAdmissionPolicy`, `CoverageGuidedAdmissionPolicy`, `CohortCoherenceAdmissionPolicy` (W7-2), `CrossRoleCorroborationAdmissionPolicy` (W8), `MultiServiceCorroborationAdmissionPolicy` (W9) — achieves `accuracy_full = 0.000` and `accuracy_services = 0.000` under the decoder's set-equality `services_correct` check. The bipartite multiset for the corroborated decoy is observationally indistinguishable from at least one gold service's bipartite multiset; no service-blind policy can prefer one over the other.** | **proved-empirical (n=10) + structural sketch** | `docs/data/phase57_decoder_K8_n10.json::pooled` (every admission policy at 0.000); `docs/data/phase57_seed_sweep.json` (5/5 seeds reproduce); `vision_mvp/wevra/team_coord.py` (each policy's state shape). |
| **W10-1 (SDK v3.11)** | **Bundle-aware decoder sufficiency: pairing the SDK v3.10 W9 admission policy (`top_k = 3`, `min_corroborated_roles = 2`) with `BundleAwareTeamDecoder(cck_filter=True, role_corroboration_floor=1, fallback_admitted_size_threshold=2)` achieves `accuracy_full = 1.000` on Phase-57 default (K_auditor=8, T_auditor=256, n_eval=10, bank_seed=11), stable across 5/5 alternate bank seeds (11, 17, 23, 29, 31). Headline gap = +1.000 vs every admission-only baseline, +1.000 vs SDK v3.10 W9. The first capsule-native multi-agent coordination method that crosses the admission/decoding split.** | **proved-empirical (n=50 saturated)** | `docs/data/phase57_decoder_K8_n10.json`; `docs/data/phase57_seed_sweep.json`; `Phase57DefaultConfigTests::test_bundle_decoder_strictly_beats_w9_at_K8` |
| **W10-2 (SDK v3.11)** | **CCK structural correctness (proved by inspection): for every `root_cause` label `R` in `CAUSAL_CLAIM_KINDS_PER_ROOT_CAUSE`, (a) `CCK[R]` is non-empty iff `R` is a specific-tier root_cause (data / storage / compute / edge / network); (b) `CCK[R]` is empty for `unknown` (no filter); (c) for the generic-tier root_causes `error_spike` and `latency_spike`, `CCK[R] = {ERROR_RATE_SPIKE, LATENCY_SPIKE}` (the bundle decoder is a no-op on generic-tier root_causes, by design — this is the named scope limit of W10-1).** | **proved (by inspection)**            | `vision_mvp/wevra/team_coord.py::CAUSAL_CLAIM_KINDS_PER_ROOT_CAUSE`; `BundleAwareTeamDecoder.decode`. |
| **W10-3 (SDK v3.11)** | **Backward-compat with W7-2 / W8 / W9 on R-54 / R-55 / R-56 (proved-empirical n=10 each): the bundle-aware decoder paired with the corresponding admission policy (W7-2 buffered cohort on R-54, W8 corroboration on R-55, W9 multi-service on R-56) achieves the same `accuracy_full` (1.000) and the same `services` set as the prior best capsule strategy on every cell of every prior regime. The `fallback_admitted_size_threshold = 2` knob is the load-bearing piece: when admission has produced a small dominant set (size ≤ 2), the decoder trusts admission rather than re-filtering. Without the fallback, the decoder slips by 0.20 on Phase-56 `slow_query_web_db` scenarios where one gold service is mentioned only via non-CCK kinds.** | **proved-empirical (n=10 each)**      | `docs/RESULTS_WEVRA_BUNDLE_DECODER.md` § 3 backward-compat probe; `Phase57BackwardCompatTests::test_bundle_decoder_matches_w9_on_phase56`, `..._matches_w8_on_phase55`, `..._matches_w7_2_on_phase54`. |
| **W10-4 (SDK v3.11)** | **Decoy-CCK-promotion falsifier (proved-empirical n=10 saturated): on the Phase-57 falsifier bank where the decoy is also mentioned via at least one CCK-eligible claim_kind for the gold root_cause, the bundle decoder cannot exclude the decoy; `capsule_bundle_decoder` ties FIFO at `accuracy_full = 0.000` on 10/10 falsifier scenarios. The W10-1 win does NOT hold in this regime — by construction. Sharper observation: this is the structural limit of *any* CCK-style closed-vocabulary tier filter; a higher-resolution decoder (e.g. multi-round bundle reasoning, W10-C3) is needed.** | **proved-empirical (n=10 falsifier saturated)** | `docs/data/phase57_falsifier_K8_n10.json`; `Phase57FalsifierTests::test_bundle_decoder_ties_fifo_on_falsifier`. |

## Multi-round bundle-aware team decoder (W11-Λ, W11-1..W11-4) — SDK v3.12

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W11-Λ (SDK v3.12)** | **Single-round structural limit on R-58: on the Phase-58 default bank (round-1 generic-noise-only on (gold_A, gold_B, decoy) with cross-role-corroborated decoy; round-2 single specific-tier claim with no `service=` token), every single-round capsule strategy in the SDK — substrate, FIFO, priority, coverage, W7-2 cohort, W8 corroboration, W9 multi-service, AND W10 single-round bundle decoder — achieves `accuracy_full = 0.000`. No partition of admitted handoffs into per-round bundles can elect a specific root_cause AND retain the round-1 service-tag inventory; the priority decoder picks `latency_spike`/`error_spike` on round-1 alone, and round-2 alone has empty service-tag set.** | **proved-empirical (n=8) + structural sketch** | `docs/data/phase58_default_K8_n8.json::pooled` (every single-round strategy at 0.000); `docs/data/phase58_seed_sweep_K8_n8.json` (5/5 seeds reproduce); `vision_mvp/wevra/team_coord.py` (decoder priority + CCK closed-vocabulary tables). |
| **W11-1 (SDK v3.12)** | **Multi-round bundle decoder sufficiency: pairing FIFO admission with `MultiRoundBundleDecoder(noise_decoy_role_floor=2, inner=BundleAwareTeamDecoder(cck_filter=False))` achieves `accuracy_full = 1.000` on Phase-58 default (K_auditor=8, T_auditor=256, n_eval=8, bank_seed=11), stable across 5/5 alternate bank seeds (11, 17, 23, 29, 31). Headline gap = +1.000 vs every single-round method including SDK v3.11 W10. The first capsule-native multi-agent coordination method that crosses the temporal/structural split.** | **proved-empirical (n=40 saturated across 5 seeds)** | `docs/data/phase58_default_K8_n8.json`; `docs/data/phase58_seed_sweep_K8_n8.json`; `Phase58DefaultTests::test_multi_round_strict_win`; `Phase58SeedStabilityTests::test_gap_stable_across_5_seeds`. |
| **W11-2 (SDK v3.12)** | **Round-union monotonicity (proved structural): let `H_1, ..., H_n` be the per-round admitted bundles and `K(B)` the set of `claim_kinds` in `B`. Then `K(union) ⊇ K(H_i)` for every `i`, and the priority decoder's elected `root_cause` on `union` is at most as generic as on any individual `H_i` in the priority order. Adding more `claim_kinds` to the input of a priority-rule decoder can only move the elected label up the priority list, not down.** | **proved (by construction)**       | `vision_mvp/wevra/team_coord.py::_decoded_root_cause`, `_DECODER_PRIORITY`; structural argument. |
| **W11-3 (SDK v3.12)** | **Backward-compat with W7-2 / W8 / W9 / W10 on R-54 / R-55 / R-56 / R-57 (proved-empirical n=8 each): the cross-regime summary preserves the prior anchor `accuracy_full = 1.000` on every prior regime byte-for-byte (`cohort_buffered` on R-54, `corroboration` on R-55, `multi_service` on R-56, `bundle_decoder` on R-57). The W11 decoder is a strict generalisation along the noise-corroboration axis only; on regimes where the decoy is NOT noise-corroborated or where admission already filters the dominant set to size ≤ 2, the inner W10 fallback path preserves the prior win.** | **proved-empirical (n=8 each)**    | `docs/data/phase58_cross_regime.json`; `Phase58CrossRegimeTests::test_R54_W7_2_preserved`, `..._R55_W8_preserved`, `..._R56_W9_preserved`, `..._R57_W10_preserved`. |
| **W11-4 (SDK v3.12)** | **Round-budget falsifier (proved-empirical n=8 saturated): on the Phase-58 falsifier bank where round-1 noise count is doubled, paired with `K_auditor=4`, the round-1 noise floods the per-round inbox and round-2's specific-tier claim's downstream effect on the union is structurally limited; `capsule_multi_round` ties FIFO at `accuracy_full = 0.000` on 8/8 falsifier scenarios. The W11-1 win does NOT hold in this regime — by construction. Sharper observation: this is the structural limit of *any* multi-round decoder that consumes per-round admitted sets; a smarter admission policy that prioritises specific-tier kinds is the natural next move.** | **proved-empirical (n=8 falsifier saturated)** | `docs/data/phase58_falsifier_K4_n8.json`; `Phase58FalsifierTests::test_falsifier_collapses_to_zero`. |

## Real-LLM-robust multi-round bundle decoder (W12-Λ, W12-1..W12-4) — SDK v3.13

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W12-Λ (SDK v3.13)** | **Real-LLM single-round / un-normalised structural limit on R-59: on the Phase-59 default bank (delayed-causal-evidence shape from R-58 + LLM-shaped extractor at default ``synonym_prob=0.50, svc_token_alt_prob=0.30``), every un-normalised capsule strategy in the SDK — substrate, FIFO, priority, coverage, W7-2 cohort, W8 corroboration, W9 multi-service, W10 single-round bundle decoder, AND **SDK v3.12 W11 ``MultiRoundBundleDecoder``** — achieves `accuracy_full = 0.000`. The synonym drift breaks every priority-decoder kind lookup; the elected root_cause stays generic; the W11 contradiction-aware drop never fires. Holds across 5/5 (bank_seed, llm_seed) values.** | **proved-empirical (n=12) + structural sketch** | `docs/data/phase59_default_K8_n12.json::pooled` (every un-normalised strategy at 0.000); `docs/data/phase59_seed_sweep_K8_n12.json` (5/5 seeds); `vision_mvp/wevra/team_coord.py::_DECODER_PRIORITY` (closed-vocabulary kind lookup). |
| **W12-1 (SDK v3.13)** | **RobustMultiRoundBundleDecoder sufficiency under bounded LLM noise: pairing FIFO admission with `RobustMultiRoundBundleDecoder()` (closed-vocabulary :data:`CLAIM_KIND_SYNONYMS` + payload-rewrite layer ahead of `MultiRoundBundleDecoder`) achieves `accuracy_full = 1.000` on Phase-59 default (K_auditor=8, T_auditor=256, n_eval=12, bank_seed=11, llm_synonym_prob=0.50, llm_svc_alt_prob=0.30), stable across 5/5 alternate (bank_seed, llm_seed) values (11, 17, 23, 29, 31). Headline gap robust − fifo = +1.000 on every seed; gap robust − w11 ∈ {0.750, 0.917, 1.000} per seed (min 0.750 at seed 23, well above the 0.50 stability threshold). The first capsule-native multi-agent coordination method that crosses the synthetic→real-LLM transfer boundary.** | **proved-conditional (premise: noise budget ⊆ closed-vocabulary closure) + proved-empirical (n=60 saturated across 5 seeds)** | `docs/data/phase59_default_K8_n12.json`; `docs/data/phase59_seed_sweep_K8_n12.json`; `Phase59DefaultTests::test_robust_strict_win`; `Phase59SeedStabilityTests::test_gap_holds_across_5_seeds`. |
| **W12-2 (SDK v3.13)** | **Closed-vocabulary normalisation soundness (proved by inspection): (a) every entry in :data:`CLAIM_KIND_SYNONYMS` maps to a canonical kind in `_DECODER_PRIORITY` (mechanically tested by `NormalisationTableTests::test_every_synonym_target_is_canonical`); (b) every canonical kind is its own synonym (idempotency of `normalize_claim_kind` on canonical input); (c) every variant the noisy LLM extractor can emit (in :data:`NOISY_KIND_VARIANTS`) is in the synonym table — the closure property — and resolves to the matching canonical (mechanically tested by `NoisyExtractorTests::test_noisy_variants_all_in_synonym_table`); (d) the payload-rewrite patterns in :data:`_SERVICE_TAG_REWRITES` are idempotent on payloads already in the canonical ``service=<tag>`` form.** | **proved (by inspection) + mechanically-checked** | `vision_mvp/wevra/team_coord.py::CLAIM_KIND_SYNONYMS`, `_SERVICE_TAG_REWRITES`; `NormalisationTableTests`. |
| **W12-3 (SDK v3.13)** | **Backward-compat with R-58 / R-59-clean (proved-empirical, n=8 each): on the Phase-58 default bank and on Phase-59 with ``llm_mode='synthetic_clean_llm'``, the rewrite counters (`last_n_kind_rewrites`, `last_n_payload_rewrites`) are zero and the W12 decoder produces byte-for-byte the same `(root_cause, services, remediation)` as the W11 `MultiRoundBundleDecoder`. Cross-regime: R-54 / R-55 / R-56 / R-57 / R-58 anchor strategies all still hit `accuracy_full = 1.000`.** | **proved-empirical (n=8 each + cross-regime audit)** | `docs/data/phase59_clean_K8_n8.json`; `docs/data/phase59_cross_regime.json`; `Phase59BackwardCompatTests::test_clean_mode_robust_matches_w11`, `..._cross_regime_R54_to_R58_preserved`; `RobustDecoderUnitTests::test_clean_input_matches_w11`. |
| **W12-4 (SDK v3.13)** | **Out-of-vocabulary noise-budget falsifier (proved-empirical n=8 saturated): on the Phase-59 falsifier bank (``oov_prob = 0.50`` set in ``run_phase59`` when ``use_falsifier_bank=True``; OOV kinds named in :data:`OUT_OF_VOCAB_KINDS` and verified absent from :data:`CLAIM_KIND_SYNONYMS`), `capsule_robust_multi_round` ties FIFO at `accuracy_full = 0.000` on 8/8 scenarios. The W12-1 win does NOT hold in this regime — by construction. Sharper observation: this is the structural limit of *any* closed-vocabulary normalisation table; expanding the table (adding a row for ``DEADLOCK_PROBABLY_DETECTED_MAYBE``) is a research move, not a structural fix — and the next variant the LLM emits will fall outside the new table.** | **proved-empirical (n=8 falsifier saturated)** | `docs/data/phase59_falsifier_K8_n8.json`; `Phase59FalsifierTests::test_W12_4_oov_collapses_to_zero`; `RobustDecoderUnitTests::test_oov_kind_does_not_get_rescued`; `NoisyExtractorTests::test_oov_kinds_are_not_in_synonym_table`. |

## Layered open-world normalisation (W13-Λ-fixed, W13-Λ-real, W13-1..W13-4) — SDK v3.14

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W13-Λ-fixed (SDK v3.14)** | **Fixed-closed-vocabulary normalisation has a finite closure: on R-60-wide (Phase-60 ``synthetic_wide_oov_llm`` default at ``wide_oov_prob=0.50``), every drifted variant the producer emits is in :data:`HEURISTIC_RESCUABLE_OOV_KINDS` and is *not* in :data:`CLAIM_KIND_SYNONYMS`. Therefore :class:`RobustMultiRoundBundleDecoder` (W12) ties FIFO at ``accuracy_full = 0.000``. Holds across 5/5 (bank_seed, llm_seed) values; the closure-membership property is mechanically verified by ``W13ClosureTests::test_every_wide_oov_variant_outside_w12_inside_w13``.** | **proved-empirical (n=12) + proved-by-inspection** | `docs/data/phase60_default_K8_n12.json::pooled` (W12 robust at 0.000); `vision_mvp/wevra/team_coord.py::CLAIM_KIND_SYNONYMS` (table); `vision_mvp/experiments/phase60_open_world_normalization.py::HEURISTIC_RESCUABLE_OOV_KINDS`. |
| **W13-1 (SDK v3.14)** | **Layered normaliser sufficiency under bounded-but-OOV producer noise: pairing FIFO admission with :class:`LayeredRobustMultiRoundBundleDecoder` (default config — exact :data:`CLAIM_KIND_SYNONYMS` then :data:`_HEURISTIC_KIND_RULES` then optional abstention) achieves ``accuracy_full = 1.000`` on R-60-wide default (K_auditor=8, T_auditor=256, n_eval=12, bank_seed=11, ``wide_oov_prob=0.50``), stable across 5/5 alternate (bank_seed, llm_seed) values (11, 17, 23, 29, 31) with strict gap ``layered − w12 ∈ {0.917, 1.000}`` (min 0.917, well above the 0.50 stability threshold). Headline gap layered − fifo = +1.000 on every seed; layered − multi_round = +1.000 on every seed. The first capsule-native multi-agent coordination method that crosses the open-world normalisation boundary on a regime where fixed-vocabulary normalisation is provably insufficient (W13-Λ-fixed).** | **proved-conditional (premise: noise channel ⊆ heuristic closure) + proved-empirical (n=60 saturated across 5 seeds)** | `docs/data/phase60_default_K8_n12.json`; `docs/data/phase60_seed_sweep_K8_n12.json`; `Phase60DefaultTests::test_layered_strict_win`; `Phase60SeedStabilityTests::test_layered_minus_robust_gap_holds_across_5_seeds`. |
| **W13-2 (SDK v3.14)** | **Heuristic abstraction soundness (proved by inspection + mechanically-checked): (a) every rule in :data:`_HEURISTIC_KIND_RULES`'s ``canonical`` field is a key in ``_DECODER_PRIORITY``; (b) every canonical kind self-maps through the layered normaliser (idempotency); (c) when the exact-table layer is removed, every canonical kind still self-maps via the heuristic layer (the rules witness their own canonicals). The heuristic closure is therefore *strictly larger* than the exact-table closure on every input and *equal* to the exact-table closure on canonical input.** | **proved (by inspection) + mechanically-checked** | `vision_mvp/wevra/team_coord.py::_HEURISTIC_KIND_RULES`, `LayeredClaimNormalizer`; `HeuristicRuleSoundnessTests::test_every_rule_canonical_is_in_priority_table`, `..._test_every_canonical_self_maps_through_layered`, `..._test_heuristic_layer_idempotent_on_canonical_when_exact_disabled`. |
| **W13-3 (SDK v3.14)** | **Backward compat with R-58 / R-59 / R-60-clean / R-54..R-57 (proved-empirical, n=8 each + cross-regime audit): on the Phase-58 default bank, on Phase-59 noisy default, on Phase-60 ``synthetic_clean_llm``, on Phase-60 ``synthetic_synonym_llm``, and on R-54..R-57 default banks, the W13 layered decoder produces byte-for-byte the same answer as the W11 / W12 decoder (the heuristic layer fires zero times when the exact layer covers all drift). Cross-regime: R-54 / R-55 / R-56 / R-57 / R-58 anchor strategies all still hit ``accuracy_full = 1.000``.** | **proved-empirical (n=8 each + cross-regime audit)** | `docs/data/phase60_clean_K8_n8.json`; `docs/data/phase60_cross_regime.json`; `Phase60BackwardCompatTests::test_clean_mode_layered_matches_w11`, `..._test_synonym_mode_layered_matches_w12`, `..._test_cross_regime_R54_to_R59_preserved`. |
| **W13-4 (SDK v3.14)** | **Open-world closure boundary (proved-empirical, n=8 saturated): on R-60-cosmic (Phase-60 ``synthetic_cosmic_oov_llm`` default at ``cosmic_oov_prob=1.0``; the round-2 specific-tier kind is replaced by an entry from :data:`COSMIC_OOV_KINDS` such as ``XYZZY_QQQQ`` or ``COSMIC_RAY_FLIP`` that is verified absent from both :data:`CLAIM_KIND_SYNONYMS` AND :data:`_HEURISTIC_KIND_RULES`'s pattern union), :class:`LayeredRobustMultiRoundBundleDecoder` ties FIFO at ``accuracy_full = 0.000`` on 8/8 scenarios. The W13-1 win does NOT hold in this regime — by construction. Sharper observation: this is the *structural limit of any predicate-based normaliser*: any closed-vocabulary set of regex patterns has a finite firing surface; OOV inputs whose surface form witnesses none of the patterns escape the closure. A learned normaliser (W13-C2) widens but does not eliminate the closure.** | **proved-empirical (n=8 falsifier saturated)** | `docs/data/phase60_falsifier_K8_n8.json`; `Phase60FalsifierTests::test_W13_4_cosmic_oov_collapses_layered_to_zero`; `W13ClosureTests::test_every_cosmic_oov_variant_escapes_both_layers`. |
| **W13-Λ-real (SDK v3.14; honest negative)** | **Real Ollama 14B (qwen2.5:14b-32k on Mac 1) at temperature 0 on the calibrated Phase-58 incident-triage prompt, by default, does NOT generate the R-58 delayed-causal-evidence bench property: across 4 scenarios × 12 producer calls, the LLM emits 0 drifted kinds (every claim_kind is canonical and in :data:`CLAIM_KIND_SYNONYMS`'s key set as a self-map) and filters low-magnitude decoy events as noise (the ``monitor`` role emits ``NONE`` for the deliberately-low-magnitude decoy events, breaking the cross-role decoy corroboration assumption). The bench property holds in 0/4 scenarios; normalisation has nothing to rescue (W13 stats: ``n_heuristic = 0``, ``n_exact > 0``, ``n_passthrough = 0``); W13 ties W12 ties multi_round at ``accuracy_full = 0.250``. The W13 advance is *not* observable on R-60-ollama because there is no drift to widen the closure of. The honest reading: synthetic→real-LLM transfer at the open-world axis is gated NOT primarily by normalisation but by *event-shape design + prompt-side discipline*; the synthetic noisy extractor mimics drift but does not mimic real-LLM epistemic filtering.** | **empirical-research (n=4 producer-side observation; 12 real Ollama calls; 0 failures; 49.4 s wall on Mac 1)** | `docs/data/phase60_real_ollama_qwen2_5_14b_n4.json::raw_responses_per_scenario`, `extractor_stats`; `vision_mvp/experiments/phase60_open_world_normalization.py::CapturingOllamaExtractor`. |

## Producer-side ambiguity preservation (W14-Λ-prompt, W14-Λ-real, W14-1..W14-4) — SDK v3.15

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W14-Λ-prompt (SDK v3.15)** | **Producer-side ambiguity-erasure structural limit on R-61-naive-prompt: when the producer collapses the bench property upstream — by magnitude-filtering low-magnitude decoy events and/or by compressing round-1 toward a single best diagnosis — every capsule strategy in the SDK (substrate, FIFO, priority, coverage, W7-2 cohort, W8 corroboration, W9 multi-service, W10 single-round bundle, W11 multi-round, W12 robust-multi-round, AND **SDK v3.14 W13 layered-multi-round**) achieves ``accuracy_full = 0.000``. The bench property's ``round1_decoy_corroborated`` predicate requires ≥ 2 distinct producer roles to mention the decoy in round 1; under producer-side compression the auditor receives ≤ 1 distinct-role decoy mention; a never-emitted handoff has no CID (Capsule Contract C5); no role view can admit it (W7-3 extraction floor); no cross-round decoder can decode it. Calibration: the synthetic ``MagnitudeFilteringExtractor`` on the *original* Phase-58 events reproduces the W13-Λ-real real-Ollama ``round1_decoy_corroborated`` count of 0/4 byte-for-byte.** | **proved-empirical (n=8 saturated × 5 seeds) + structural sketch via W7-3 extension** | `docs/data/phase61_naive_prompt_K8_n8.json::pooled` (every strategy at 0.000); `docs/data/phase61_seed_sweep_naive_K8_n8.json` (5/5 seeds reproduce); `Phase61DefaultTests::test_naive_prompt_property_collapses`; `Phase61DefaultTests::test_naive_prompt_every_method_ties_fifo`. |
| **W14-1 (SDK v3.15)** | **StructuredProducerProtocol sufficiency under bounded producer compression: pairing the :class:`StructuredProducerProtocol` (mode = STRUCTURED) with the same magnitude-filter producer simulation AND the W11/W12/W13 cross-round bundle decoder achieves ``accuracy_full = 1.000`` on R-61-structured-prompt default (K_auditor=8, T_auditor=256, n_eval=8, bank_seed=11), stable across 5/5 alternate ``bank_seed`` values (11, 17, 23, 29, 31) with strict gap structured − naive ∈ {1.000, 1.000, 1.000, 1.000, 1.000} (min 1.000 well above the 0.50 stability threshold). On real Mac-1 ``qwen2.5:14b-32k`` at n=8, the same pairing achieves ``accuracy_full = 0.500`` while the naive-prompt counterpart ties FIFO at 0.000 — a +0.500 strict separation on the real-LLM stream that clears the R-61-OLLAMA-A tier. The first capsule-native multi-agent coordination method that strictly improves over substrate FIFO on a real-LLM stream by ≥ 0.50.** | **proved-conditional (premise: comparable-magnitude events + structured prompt + cross-round capsule pipeline) + proved-empirical (synthetic n=40 saturated × 5 seeds; real Ollama n=24 producer calls × 8 scenarios × 2 prompt modes)** | `docs/data/phase61_structured_prompt_K8_n8.json`; `docs/data/phase61_seed_sweep_structured_K8_n8.json`; `docs/data/phase61_real_ollama_structured_qwen2_5_14b_n8.json`; `Phase61DefaultTests::test_structured_prompt_layered_strict_win`; `Phase61SeedStabilityTests::test_gap_structured_minus_naive_holds_across_5_seeds`. |
| **W14-2 (SDK v3.15)** | **Schema soundness + protocol determinism (proved by inspection + mechanically-checked): (a) every role's ``observation_kinds ∪ diagnosis_kinds ⊆ allowed_kinds``; (b) ``observation_kinds ∩ diagnosis_kinds = ∅``; (c) ``observation_kinds`` is a subset of the global generic-noise tier ``INCIDENT_TRIAGE_OBSERVATION_KINDS``; (d) :func:`StructuredProducerProtocol.render_prompt` is byte-for-byte deterministic given (schema, events, round_idx); (e) naive-mode rendering produces text containing the legacy phrases (``operational symptoms``, ``Maximum 6 lines``); structured-mode rendering produces text containing the W14 markers (``ROUND 1 — OBSERVATION MODE``, ``EMIT ONE CLAIM PER LISTED EVENT``, ``FORBIDDEN claim kinds``).** | **proved (by inspection) + mechanically-checked** | `vision_mvp/wevra/team_coord.py::RoleExtractionSchema`, `StructuredProducerProtocol`, `incident_triage_role_schemas`; `IncidentTriageSchemaTests`; `StructuredProtocolDeterminismTests`. |
| **W14-3 (SDK v3.15)** | **Backward compatibility (proved-empirical, full programme-wide regression): the W14 protocol surface is purely additive. On R-58 / R-59 / R-60 default + falsifier banks and on R-54..R-57 default banks, no existing test fails; every prior anchor preserved at ``accuracy_full = 1.000`` (where prior); R-60-cosmic-OOV preserved at 0.000 (W13-4 closure boundary). The naive-mode prompt rendering is byte-for-byte equal to the legacy ``vision_mvp.experiments.phase59_real_llm_multi_round._round_ollama_prompt`` helper. 393/393 prior tests pass.** | **proved-empirical (full programme-wide regression)** | `vision_mvp/tests/` (full suite); `docs/data/phase60_cross_regime.json`; `Phase61CrossRegimeTests::test_three_subbanks_separate_cleanly`. |
| **W14-4 (SDK v3.15)** | **Combined-intervention falsifier (proved-empirical): on R-61-ollama-naive (real Mac-1 ``qwen2.5:14b-32k`` on the comparable-magnitude events under the *naive* prompt), the bench property holds in 0/8 scenarios; every capsule strategy ties FIFO at 0.000. The W14-1 win does NOT hold in this regime — by construction. **Both** the comparable-magnitude event redesign AND the structured prompt are required to clear the W14-1 strong bar; either alone is insufficient. Sharper observation: the producer-protocol layer is a *boundary-side* intervention; without the bench-property-supporting events, the protocol has nothing to preserve. Without the protocol, the comparable-magnitude events still get compressed by the LLM. The two interventions compose multiplicatively, not additively.** | **proved-empirical (n_eval=8 × 24 real producer calls saturated)** | `docs/data/phase61_real_ollama_naive_qwen2_5_14b_n8.json`. |
| **W14-Λ-real (SDK v3.15; honest positive)** | **Real Mac-1 ``qwen2.5:14b-32k`` at ``temperature=0`` on the Phase-61 redesigned comparable-magnitude events: under the *naive* prompt the bench property holds in 0/8 scenarios and every capsule strategy ties FIFO at 0.000 (empirical confirmation of W14-Λ-prompt on real LLM); under the *structured* prompt the bench property holds in 7/8 scenarios, ``capsule_layered_multi_round = capsule_robust_multi_round = capsule_multi_round = 0.500``, ``layered − fifo = +0.500`` pooled accuracy_full at exactly the R-61-OLLAMA-A threshold, audit T-1..T-7 preserved on every cell. The R-61-OLLAMA-A tier of the success criterion ``§ 1.5`` is cleared; the 1/8 model-error failure (LLM judged ``error_rate=0.15`` not to qualify as ``ERROR_RATE_SPIKE``) is on the LLM, not on the protocol — captured by W14-C2.** | **empirical-research (n=8 × 24 real Ollama calls under each prompt mode; 0 endpoint failures; 111.4 s wall structured + 79.9 s wall naive on Mac 1)** | `docs/data/phase61_real_ollama_structured_qwen2_5_14b_n8.json::raw_responses_per_scenario`, `extractor_stats`, `pooled`; `docs/data/phase61_real_ollama_naive_qwen2_5_14b_n8.json`. |

## Attention-aware capsule context packing (W15-Λ-budget, W15-Λ-degenerate, W15-1..W15-4) — SDK v3.16

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W15-Λ-budget (SDK v3.16)** | **Decoder-side budget structural limit on R-62-tightbudget under FIFO packing: when the cross-round decoder's bundle is FIFO-truncated under ``T_decoder = 24`` (strictly below the admitted union token sum ≈ 27 / 28 per scenario), every cross-round decoder in the SDK ties FIFO at ``accuracy_full = 0.000``. The bench property's R-58 round-2 disambiguator predicate requires ``root_cause`` to come from a specific-tier claim; under FIFO arrival order the round-1 generic-noise emissions arrive first and the round-2 specific-tier claim arrives last; under ``T_decoder`` that fits |round-1| but not the round-2 disambiguator, the FIFO-packed bundle contains only round-1 generic noise; the priority decoder elects a generic ``root_cause``; the CCK is generic; the W11 contradiction-aware drop has nothing specific to project; the answer is wrong by construction. Extends W7-3 (extraction floor) to the decoder-side axis: a never-decoded handoff has no influence on the answer.** | **proved-empirical (n=8 saturated × 5 seeds) + structural sketch via W7-3 extension to the decoder-side axis** | `docs/data/phase62_tightbudget_K12_n8.json::pooled` (capsule_layered_fifo_packed at 0.000); `docs/data/phase62_seed_sweep_tightbudget_K12_n8.json` (5/5 seeds reproduce); `Phase62DefaultTests::test_tightbudget_fifo_pack_ties_substrate_fifo`; `Phase62DefaultTests::test_tightbudget_fifo_pack_drops_round2_specific_claim`. |
| **W15-1 (SDK v3.16)** | **AttentionAwareBundleDecoder sufficiency under bounded T_decoder with hypothesis preservation: pairing the :class:`AttentionAwareBundleDecoder` (default :class:`CapsuleContextPacker` with ``preserve_hypotheses=True`` and pre-committed weight defaults ``w_tier=4.0``, ``w_cck=3.0``, ``w_corroboration=1.0``, ``w_magnitude=0.1``, ``w_round=0.5``) with the W13 layered decoder achieves ``accuracy_full = 1.000`` on R-62-tightbudget default (K_auditor=12, T_auditor=256, T_decoder=24, n_eval=8, bank_seed=11), stable across 5/5 alternate ``bank_seed`` values (11, 17, 23, 29, 31) with strict gap attention − fifo_packed_layered ∈ {1.000, 1.000, 1.000, 1.000, 1.000} (min 1.000 well above the 0.50 stability threshold). The W15 packer puts the round-2 specific-tier claim at rank 0 of the kept bundle (``position_of_first_causal_claim_avg = 0.0``) in 8/8 scenarios; the FIFO packer keeps the round-2 claim in 0/8 scenarios. The win is **conditional** on (a) the bench property holding, (b) ``T_decoder`` < admitted-union token sum, AND (c) the regime carrying round-2 specific-tier disambiguation that FIFO would push past the budget; either condition removed collapses the win. The first capsule-native multi-agent coordination method that strictly improves over a FIFO-packed cross-round decoder under decoder-side budget pressure by +1.000.** | **proved-conditional (premises = R-62 bench property + ``T_decoder`` < admitted-union token sum + R-58-shape round-2 disambiguator) + proved-empirical (synthetic n=40 saturated × 5 seeds)** | `docs/data/phase62_tightbudget_K12_n8.json`; `docs/data/phase62_seed_sweep_tightbudget_K12_n8.json`; `Phase62DefaultTests::test_tightbudget_w15_strict_win_over_fifo_pack`; `Phase62DefaultTests::test_tightbudget_w15_keeps_round2_specific_claim`; `Phase62SeedStabilityTests::test_gap_attention_minus_fifo_pack_holds_across_5_seeds`. |
| **W15-2 (SDK v3.16)** | **Pack determinism + closed-form salience (proved by inspection + mechanically-checked): (a) salience score is a closed-form deterministic function of the handoff's bytes plus the elected ``root_cause`` (no RNG, no wall-clock, no environment dependence); (b) the pack output (kept handoffs and their order) is byte-deterministic given the input handoffs + ``root_cause`` + ``T_decoder`` + ``round_index_hint``; (c) tie-break is canonical ``(-salience, round_idx, source_role, claim_kind, payload_sha)``; byte-identical handoffs collapse to one slot; (d) :class:`FifoContextPacker` preserves arrival order under ``T_decoder = None``; (e) the hypothesis-preservation pass guarantees every distinct ``(service_tag, source_role, tier)`` triple gets at most one slot in the kept bundle.** | **proved (by inspection) + mechanically-checked** | `vision_mvp/wevra/team_coord.py::CapsuleContextPacker`, `FifoContextPacker`, `AttentionAwareBundleDecoder`; `CapsuleContextPackerDeterminismTests`; `FifoContextPackerTests`. |
| **W15-3 (SDK v3.16)** | **Backward compatibility (proved-empirical, full programme-wide regression): with ``T_decoder = None`` the :class:`AttentionAwareBundleDecoder` reduces to the W13 layered decoder on the answer field; the ``pack_stats`` block is purely additive. On R-58 default n=8 the W15 answers match W13 byte-for-byte in 8/8 scenarios; on R-54..R-57 the admitted set is small enough that the trust-admission fallback fires identically under both decoders. R-58 / R-59 / R-60 / R-61 default + falsifier banks all preserved; 393/393 prior tests pass byte-for-byte; 37 new tests cover the W15 surface and Phase-62 closure invariants (430/430 total in the wevra suite).** | **proved-empirical (full programme-wide regression)** | `vision_mvp/tests/` (full suite); `AttentionAwareBackwardCompatTests::test_w15_no_budget_ties_w13_byte_for_byte`. |
| **W15-Λ-degenerate (SDK v3.16)** | **Saturation falsifier (proved-empirical): on R-62-saturation (``T_decoder = 9999``, effectively unbounded), the salience reordering is a no-op on the answer field; the W15 salience pack ties the FIFO pack and both tie the unpacked W13 layered decoder at ``accuracy_full = 1.000``. The W15-1 win is *not* observable on regimes without budget pressure — by construction. This is the conditionality-sharpener for the W15-1 strict-gain claim: the win lives at the decoder-budget-pressured slice of the regime grid only.** | **proved-empirical (n=8 saturated)** | `docs/data/phase62_saturation_K12_n8.json`; `Phase62DefaultTests::test_saturation_falsifier_w15_ties_fifo_pack`. |
| **W15-4 (SDK v3.16)** | **Token-efficiency floor (proved + mechanically-checked): both :class:`CapsuleContextPacker` and :class:`FifoContextPacker` strictly enforce ``tokens_kept ≤ T_decoder`` for every input on every call (no soft truncation; no over-budget pack). Equivalently ``tokens_kept_over_input ≤ 1.0``. The pack-stats block is a faithful summary: ``n_input = n_kept + n_dropped_budget`` and ``tokens_input = tokens_kept + sum(n_tokens for h in dropped)``. On R-62-tightbudget the W15 packer keeps 186/220 tokens (84.6%) and 76/88 handoffs (86.4%); the FIFO packer keeps 192/220 tokens (87.3%) and 80/88 handoffs (90.9%) — the FIFO packer keeps a marginally larger but *causally-misaligned* subset (round-2 specific claim absent in 8/8 cells).** | **proved (by inspection) + mechanically-checked** | `vision_mvp/wevra/team_coord.py::CapsuleContextPacker.pack`, `FifoContextPacker.pack`; `Phase62DefaultTests::test_w15_token_efficiency_under_tight_budget`. |

## End-to-end W14 + W15 composition (W16-Λ-compose, W16-Λ-degenerate, W16-Λ-real-replay, W16-1..W16-3) — SDK v3.17

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W16-Λ-compose (SDK v3.17)** | **Joint producer + decoder structural limit on R-63-naive-tight (Phase-62 multi-hypothesis events × Phase-61 magnitude-filter naive producer × ``T_decoder = 24``): every capsule strategy in the SDK — substrate, FIFO, priority, coverage, W7-2 cohort, W8 corroboration, W9 multi-service, W10 single-round bundle, W11 multi-round, W12 robust-multi-round, W13 layered-multi-round, W14-only-budgeted (W13 + FIFO pack), AND W15 (AttentionAwareBundleDecoder over the naive-prompt stream) — achieves ``accuracy_full = 0.000``. Two structurally orthogonal limits fire simultaneously: (i) W14-Λ-prompt — magnitude-filter naive producer compresses round-1 to top-1-per-(role, kind), erasing the cross-role decoy corroboration; (ii) W15-Λ-budget — ``T_decoder`` strictly below the admitted union token sum drops the round-2 specific-tier disambiguator under FIFO truncation. The named limits compose multiplicatively: closing one alone is insufficient. The recorded Phase-61 naive-prompt bytes confirm the joint failure on a real LLM (``r63_ollama_replay_naive_tight`` pooled ``capsule_attention_aware = 0.000`` at T_decoder = 14).** | **proved-empirical (n=8 saturated synthetic + recorded n=8 × 24 real-LLM bytes) + structural sketch via composition of W14-Λ-prompt and W15-Λ-budget** | `docs/data/phase63_cross_regime.json::r63_naive_tight` (every method 0.000); `docs/data/phase63_cross_regime.json::r63_ollama_replay_naive_tight` (recorded LLM bytes); `Phase63ComposedTightTests::test_naive_tight_w16_lambda_compose`; `Phase63OllamaReplayTests::test_replay_naive_tight_joint_failure`. |
| **W16-1 (SDK v3.17)** | **Composed sufficiency under producer compression + bounded T_decoder: pairing the W14 :class:`StructuredProducerProtocol` (driving the magnitude-filter producer simulation) with the W15 :class:`AttentionAwareBundleDecoder` (default :class:`CapsuleContextPacker` with ``preserve_hypotheses=True`` and pre-committed weight defaults ``w_tier=4.0, w_cck=3.0, w_corroboration=1.0, w_magnitude=0.1, w_round=0.5``) achieves ``accuracy_full = 1.000`` on R-63-COMPOSED-TIGHT default (K_auditor=12, T_auditor=256, T_decoder=24, n_eval=8, bank_seed=11), stable across 5/5 alternate ``bank_seed`` values (11, 17, 23, 29, 31) with strict gap composed − fifo_packed_layered ∈ {1.000, 1.000, 1.000, 1.000, 1.000} (min 1.000 well above the 0.50 stability threshold). The win is **conditional** on (a) the bench property holding under the structured prompt, (b) ``T_decoder`` below the admitted-union token sum, AND (c) ``T_decoder`` *above* the round-2 specific-tier claim's token cost (so the disambiguator can fit). Removing any one collapses to a named falsifier (W14-Λ-prompt / W15-Λ-budget / W16-Λ-degenerate). The first end-to-end composition method that strictly improves over every non-composed baseline (W14-only-budgeted, W15-only-without-W14, substrate FIFO) by +1.000 on a regime where both the W14 and W15 layers are independently load-bearing.** | **proved-conditional (premises = R-62 bench property under structured prompt + ``T_decoder`` ∈ (round-2 disambiguator token cost, admitted union token sum)) + proved-empirical (synthetic n=40 saturated × 5 seeds)** | `docs/data/phase63_composed_tight_K12_n8.json`; `docs/data/phase63_seed_sweep_composed_K12_n8.json` (5/5 seeds); `Phase63ComposedTightTests::test_composed_strict_win_over_fifo_pack`; `Phase63ComposedTightTests::test_composed_strict_win_over_substrate_fifo`; `Phase63ComposedTightTests::test_composed_keeps_round2_specific_claim`; `Phase63SeedStabilityTests::test_gap_composed_minus_fifo_pack_holds_across_5_seeds`. |
| **W16-2 (SDK v3.17)** | **Sub-additivity / multiplicative composition: on R-63-COMPOSED-TIGHT default, neither W14 alone (via the W13 + FIFO-pack baseline; ``capsule_layered_fifo_packed`` at 0.000) nor W15 alone (via the AttentionAwareBundleDecoder over a naive-prompt stream; the ``capsule_attention_aware`` cell of R-63-naive-tight at 0.000) clears the bar. Therefore ``composed_gain = 1.000 > 0 = W14_alone_gain + W15_alone_gain``. The composition is *strictly multiplicative* — each layer is necessary on the regime where the other layer's limit fires; W14 closes the upstream emission gap, W15 closes the downstream retention gap, and only the simultaneous application clears the bar. This is the formal coupling statement: producer-side ambiguity preservation and decoder-side capsule context packing are *jointly necessary* on the composed regime; closing one without the other leaves the other's limit firing.** | **proved-empirical (n=8 saturated)** | `docs/data/phase63_cross_regime.json` (every cell separates cleanly); `Phase63ComposedTightTests::test_composed_strict_win_over_fifo_pack`; `Phase63ComposedTightTests::test_naive_tight_w16_lambda_compose`; `Phase63CrossRegimeTests::test_seven_synthetic_cells_separate_cleanly`. |
| **W16-3 (SDK v3.17)** | **Backward compatibility (proved-empirical, full programme-wide regression): with ``extractor = identity, prompt_mode = naive, T_decoder = None`` the Phase-63 pipeline reduces to the W13 layered decoder byte-for-byte on the answer field; with ``extractor = magnitude_filter, prompt_mode = structured, T_decoder = None`` the pipeline reduces to the W14-1 anchor on R-61. R-58 / R-59 / R-60 / R-61 / R-62 default + falsifier banks all preserved; **442/442 prior tests pass byte-for-byte**; 22 new tests cover the W16 surface (replay extractor, Phase-63 default config, 5-seed stability, 7-cell cross-regime, 4-cell ollama-replay).** | **proved-empirical (full programme-wide regression)** | `vision_mvp/tests/` (full suite, 442/442 pass); `Phase63ComposedTightTests::test_baseline_loose_w15_ties_layered`; `Phase63ComposedTightTests::test_w14_success_loose_budget`. |
| **W16-Λ-degenerate (SDK v3.17)** | **Extreme-budget falsifier: on R-63-degen-budget (``extractor = magnitude_filter, prompt_mode = structured, T_decoder = 2``), the structured prompt restores the bench property but the budget is below the round-2 specific claim's token cost (the disambiguator alone is ≥ 6 tokens). Both ``capsule_attention_aware`` and ``capsule_layered_fifo_packed`` collapse to ``accuracy_full = 0.000``. The W16-1 win is *not* observable when ``T_decoder`` is below the disambiguator's token cost — by construction. This is the conditionality-sharpener for the W16-1 strict-gain claim: the win lives at the budget band where ``round-2 disambiguator token cost < T_decoder < admitted union token sum``.** | **proved-empirical (n=8 saturated)** | `Phase63ComposedTightTests::test_degen_budget_w16_lambda_degenerate`; `docs/data/phase63_cross_regime.json::r63_degen_budget`. |
| **W16-Λ-real-replay (SDK v3.17; honest positive)** | **Composed strict gain on recorded real-LLM bytes (empirical-research): replaying the Phase-61 ``ollama_structured`` capture (``docs/data/phase61_real_ollama_structured_qwen2_5_14b_n8.json``, n=8 × 24 producer calls, byte-stable; recorded under qwen2.5:14b-32k at temperature 0) through the W14+W15 pipeline at K_auditor=8, T_decoder=14 achieves ``capsule_attention_aware = 0.500`` while ``capsule_layered_fifo_packed = 0.000`` — **+0.500 strict gain** over the FIFO-packed-W14-only baseline on a real-LLM stream. The budget band where the gain holds is ``T_decoder ∈ [13, 16]``; outside the band, both packers either fail jointly (T ≤ 10) or both succeed (T ≥ 18). Honest scope: this is *measurement* over recorded bytes, not a fresh live probe — Mac-1 endpoint at 192.168.12.191:11434 was offline (HTTP=000) at milestone capture time. The gain is bounded by the empirical envelope of the Phase-61 W14-Λ-real anchor: 7/8 bench-property hold; the 1/8 model-side judgment failure persists. The composed result *recovers* the W14-only loose-budget accuracy under tight budget pressure — the W14 result was fragile to context-window pressure, the composed result is robust. The first end-to-end real-LLM strict advance over the strongest non-composed baseline in the programme. The recorded naive-prompt bytes (``phase61_real_ollama_naive_qwen2_5_14b_n8.json``) confirm joint failure at T_decoder=14 (W16-Λ-compose anchor on real-LLM bytes; ``r63_ollama_replay_naive_tight`` pooled accuracy_full = 0.000).** | **empirical-research (replay over n=8 × 24 byte-stable producer calls; 0 endpoint failures recorded; byte-stable parsing reproducible from the saved JSON)** | `docs/data/phase63_ollama_replay_composed_tight_qwen2_5_14b_n8.json`; `docs/data/phase63_ollama_replay_loose_qwen2_5_14b_n8.json`; `docs/data/phase63_cross_regime.json::r63_ollama_replay_*`; `Phase63OllamaReplayTests::test_replay_tight_composed_strict_win`; `Phase63OllamaReplayTests::test_replay_loose_matches_phase61_w14_anchor`; `Phase63OllamaReplayTests::test_replay_naive_tight_joint_failure`. |

## Magnitude-hinted producer + live composition + symmetric-corroboration wall (W17-1, W17-Λ-no-hint, W17-Λ-naive, W17-Λ-symmetric, W17-2, W17-3, W17-C-XMODEL) — SDK v3.18

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W17-1 (SDK v3.18)** | **Magnitude-hinted live composition strict-gain: pairing :class:`StructuredProducerProtocol(mode=PRODUCER_PROMPT_MAGNITUDE_HINTED)` + :func:`incident_triage_role_schemas(magnitude_hinted=True)` with the W14 producer + W15 decoder pipeline at ``K_auditor = 8, T_auditor = 256, T_decoder = 14`` against a fresh live ``qwen2.5:14b-32k`` Ollama backend on ``build_phase61_bank(n_replicates=2, seed=11)`` (n=8) achieves ``capsule_attention_aware = 1.000`` AND ``capsule_layered_fifo_packed = 0.000`` AND ``capsule_fifo = 0.000``; +1.000 strict separation on both axes. Bench property holds in 8/8 (closing the 1/8 R-61-OLLAMA-A model-side judgment miss that persisted under the SDK v3.15 W14 anchor and the SDK v3.17 W16-Λ-real-replay anchor). The first programme result that beats the strongest non-composed baseline by ≥ 1.0 on a fresh live LLM probe.** | **proved-conditional + empirical-research (n=8 × 24 producer calls; 0 endpoint failures; 128.2 s wall on Mac-1)** | `docs/data/phase64_live_maghint_qwen2_5_14b_n8.json`; `Phase64SyntheticBaselineTests::test_baseline_layered_strict_win`. |
| **W17-Λ-no-hint (SDK v3.18; honest envelope)** | **Live legacy-structured-prompt envelope: under ``mode = PRODUCER_PROMPT_STRUCTURED`` + ``magnitude_hinted_schema = False`` + same fresh live ``qwen2.5:14b-32k`` probe + same ``T_decoder = 14, K_auditor = 8``: bench property holds in 7/8 (the slow_query_archival miss persists); ``capsule_attention_aware = 0.500``; ``capsule_layered_fifo_packed = 0.000``; +0.500 strict gain. Reproduces the W14-Λ-real / W16-Λ-real-replay envelope on a *fresh* probe rather than on recorded bytes; the load-bearing comparison shows the magnitude-hint extension (W17-1), not a re-run of the same prompt, is what closes the gap from 0.500 to 1.000.** | **empirical-research (n=8 × 24 producer calls; 0 endpoint failures)** | `docs/data/phase64_live_struct_qwen2_5_14b_n8.json`. |
| **W17-Λ-naive (SDK v3.18)** | **Live joint-failure falsifier: under ``mode = PRODUCER_PROMPT_NAIVE`` + same live probe + same budget: bench property holds in 0/8; every capsule strategy ties FIFO at 0.000. The live counterpart of W14-Λ-prompt + W15-Λ-budget joint failure (W16-Λ-compose at the live axis).** | **empirical-research (n=8 × 24 producer calls; 0 endpoint failures)** | `docs/data/phase64_live_naive_qwen2_5_14b_n8.json`. |
| **W17-Λ-symmetric (SDK v3.18; *first explicit symmetric-corroboration limit theorem*)** | **Symmetric-corroboration limit theorem: on ``build_phase64_sym_bank(n_replicates=2, seed=11)`` (synthetic, deterministic identity extractor, every service mentioned by exactly 2 distinct routed producer roles in round 1 via generic-noise kinds with comparable magnitudes; round-2 disambiguator names the gold root_cause without ``service=`` token), every capsule strategy in the SDK — substrate, FIFO, priority, coverage, W7-2 cohort, W8 corroboration, W9 multi-service, W10 single-round bundle, W11 multi-round, W12 robust-multi-round, W13 layered-multi-round, W14H-only-budgeted (W13 + FIFO pack), AND W15 (AttentionAwareBundleDecoder over the magnitude-hinted stream) — achieves ``accuracy_full = 0.000`` under both ``T_decoder = None`` AND ``T_decoder = 24``. Priority decoder still elects the right specific-tier ``root_cause`` from the round-2 disambiguator (``accuracy_root_cause = 1.000``); the failure is ``services_correct`` set-equality. Sketch: ``services_correct`` set-equality is an asymmetric oracle: the bipartite ``(role × tag, kind, magnitude)`` multiset is observationally indistinguishable for gold and decoy under symmetric corroboration; no service-blind admission policy AND no closed-form salience packer can prefer one over the other. The W11 contradiction-aware drop fires symmetrically; the W15 hypothesis-preserving pack preserves both in equal proportion; the W14H magnitude-hint is silent on symmetric ambiguity. The first explicit symmetric-corroboration limit theorem in the programme; *discharges* the prior W15-C-SYMMETRIC / W16-C-SYMMETRIC conjectures as a negative theorem.** | **proved-empirical (n=8 saturated, both ``T_decoder ∈ {None, 24}``) + structural sketch via the asymmetric-oracle argument** | `Phase64SymWallTests::test_every_capsule_strategy_ties_fifo_loose`; `Phase64SymWallTests::test_every_capsule_strategy_ties_fifo_tight`; `docs/data/phase64_cross_regime_synthetic.json`. |
| **W17-2 (SDK v3.18)** | **Magnitude-hinted prompt determinism + threshold table soundness: (a) magnitude-hinted prompt rendering is byte-for-byte deterministic given ``(schema, events, round_idx, OperationalThreshold table)``; (b) with empty ``schema.magnitude_thresholds``, rendered prompt reduces to structured prompt with anti-relative-magnitude clause appended; (c) threshold table filters to round's allowed-set (round-2 diagnosis prompts do NOT carry round-1 observation thresholds); (d) :data:`INCIDENT_TRIAGE_DEFAULT_MAGNITUDE_THRESHOLDS` exposes the same numeric thresholds as the synthetic :class:`MagnitudeFilteringExtractor` (Phase-61 calibration anchors).** | **proved (by inspection) + mechanically-checked** | `vision_mvp/wevra/team_coord.py::_render_magnitude_hinted_prompt`, `OperationalThreshold`, `INCIDENT_TRIAGE_DEFAULT_MAGNITUDE_THRESHOLDS`; `MagnitudeHintedProtocolRenderingTests`; `IncidentTriageMagnitudeHintsTests`. |
| **W17-3 (SDK v3.18)** | **Backward compatibility (full programme regression): with ``mode = PRODUCER_PROMPT_NAIVE`` or ``mode = PRODUCER_PROMPT_STRUCTURED`` AND ``magnitude_hinted_schema = False``, the W17 surface reduces to the SDK v3.15 W14 anchor byte-for-byte. On the synthetic side, swapping ``mode = 'structured'`` for ``mode = 'magnitude_hinted'`` with ``magnitude_hinted_schema = True`` produces the same downstream answer because the synthetic ``MagnitudeFilteringExtractor`` does not consult prompt thresholds. R-58 / R-59 / R-60 / R-61 / R-62 / R-63 default + falsifier banks all preserved; **442/442 prior tests pass byte-for-byte**; 20 new tests cover the W17 surface.** | **proved-empirical (full programme-wide regression)** | `vision_mvp/tests/` (full suite; 442/442 + 20 new = 462/462); `W17BackwardCompatTests::test_synthetic_mag_hinted_matches_structured_on_r61`. |
| **W17-C-XMODEL (SDK v3.18; proved-conditional + empirical-research)** | **Cross-model live composition: the W17-1 win at qwen2.5:14b-32k transfers to qwen3.5:35b-MoE on the same fresh live R-64-LIVE-MAGHINT regime under the magnitude-hinted prompt with ``think = False``. On a fresh live ``qwen3.5:35b`` probe (n=8 × 24 producer calls; 0 failures; 92.0 s wall): bench property holds in 8/8 (W17 magnitude-hint extension transfers byte-for-byte on the bench-property axis); ``capsule_attention_aware = 0.750``; ``capsule_layered_fifo_packed = 0.000``; ``capsule_fifo = 0.000``; +0.750 strict gain over both substrate FIFO AND FIFO-packed-W14H-only baseline (well above the 0.50 strong-bar threshold). The 0.250 gap relative to the 14B anchor is on the ``accuracy_root_cause`` axis: the 35B emits a different specific-tier kind on one scenario than the 14B does (a model-side specific-kind judgment artifact, not a producer-protocol failure). The first proved-conditional cross-model real-LLM strict gain in the programme.** | **proved-conditional + empirical-research (n=8 × 24 producer calls; 0 endpoint failures; 92.0 s wall on Mac-1; ``think = False`` chat-template flag)** | `docs/data/phase64_live_maghint_qwen3_5_35b_n8.json`. |

## Conjectures (W3-C*)

| Claim   | Description                                                              | Status                                       |
| ------- | ------------------------------------------------------------------------ | -------------------------------------------- |
| W3-C1   | General subsumption: every Phase-N bounded-context theorem subsumes      | conjectural                                  |
| W3-C2   | Capsule contract preserves all Phase-N substrate guarantees              | conjectural                                  |
| W3-C3   | Honest-falsification frontier (table-level invariants)                   | retracted by W3-15 cohort lift               |
| W3-C4 (legacy) | Earlier "decoder paradigm shift candidate at 0.400 / strict zero-shot" | **retracted** by W3-26 / W3-27       |
| **W3-C4 (new SDK v3.3)** | **PARSE_OUTCOME failure_kind distribution stable across LLM tags** | **superseded by W3-C6 (sharper synthetic reading)** |
| W3-C5 (legacy) | Relational-axis extension closes W3-16                              | conjectural                                  |
| **W3-C5 (new SDK v3.3)** | **Sub-intra-cell PROMPT/LLM_RESPONSE capsule slice closes the inner-loop boundary without breaking W3-34 spine equivalence** | **DISCHARGED by W3-42/W3-43/W3-44/W3-45 in SDK v3.4** |
| **W3-C6 (new SDK v3.4)** | **Synthetic-LLM cross-distribution PARSE_OUTCOME failure-kind TVD ≥ 0.5; strict→robust parser-mode shift on synthetic.unclosed = 1.000** | **empirical (reproducible from synthetic distribution library)** |
| W3-C6   | Plurality + auxiliary signal closes 0.200 priority-decoder ceiling       | partial — replaced by learned-decoder W3-19  |
| W3-C7 (strict) | Strict point-estimate Gate-1 ($\hat p \ge 0.400$) + zero-shot Gate-2 strict penalty ≤ 5pp | **retracted** by W3-26, W3-27   |
| W3-C8   | (deferred — relational decoder closes Gate 2 strictly)                   | conjectural                                  |
| W3-C9   | Refined paradigm-shift reading: $n=80$ point-estimate + zero-shot gap    | conjectural (candidate)                      |
| W3-C10  | Relational decoder level-ceiling                                                | conjectural                                  |
| **W4-C1 (SDK v3.5; SDK v3.7 conditional reading)** | **Learned per-role admission policy admits strictly fewer handoffs (12/12 seeds) and improves pooled team-decision accuracy on most train seeds (gap_full > 0 in 11/12 seeds, mean +0.054; gap_root_cause > 0 in 8/12 seeds, mean +0.032) over the strongest fixed admission baseline (coverage-guided) on the Phase-52 default config (K_auditor=8, noise=(0.10, 0.30, 0.05), PYTHONHASHSEED=0).** | **conditional**: empirical-positive on its anchor distribution (Phase-52 synthetic+noise default config, K=8, spurious=0.30 — unchanged from SDK v3.5); **falsified** at higher symbolic noise (spurious=0.50, disclosed in SDK v3.5); **falsified out-of-distribution on the Phase-53 real-LLM regime** (capsule_learned 0.400 vs fixed 0.800 on synthetic and qwen2.5:14b-32k, gap = -0.40; ties at qwen3.5:35b at 0.800/0.800; SDK v3.7, see `docs/RESULTS_WEVRA_SCALE_VS_STRUCTURE.md` § 4.4) |
| **W4-C2 (SDK v3.5)** | **Cohort-lifted role view closes W4-3 on a sub-class of scenarios**           | **conjectural** (Phase 53 candidate; falsifier: a scenario whose causal-share floor exceeds a single COHORT's max_parents) |
| **W4-C3 (SDK v3.5)** | **The capsule-layer admission rule subsumes the Phase-36 AdaptiveSubscriptionTable route-edit primitive** | **conjectural** (open) |
| **W5-C1 (SDK v3.6)** | **Parser-boundary instability is a (model architecture × prompt-format) interaction, not a model-capacity artefact. Concretely, scaling 14.8B-dense → 36B-MoE under fixed prompt format on the bundled bank flips strict-mode parser failure rate from 0/10 to 10/10.** | **empirical-research** (n=10 saturation; falsifier = a bank where the larger model strict-parses ok > 50%) |
| **W5-C2 (SDK v3.6)** | **Robust-mode parser recovery (specifically ``recovery=closed_at_eos``) is the load-bearing safety net that makes the capsule-native runtime model-class-agnostic on the bundled prompt format. Cross-model TVD on robust = 0.000 in W5-1.** | **empirical-research** (n=10 saturation; falsifier = a model whose ``unclosed_new`` cannot be salvaged) |
| **W5-C3 (SDK v3.6)** | **The capsule-native runtime's closed-vocabulary ``PARSE_OUTCOME.failure_kind`` is a *minimum sufficient* typed witness of cross-model behaviour differences — strict-mode failure-kind TVD captures in one number what manual byte-level diff over N responses would surface.** | **conjectural** (research; falsifier = a model pair with identical strict-mode `failure_kind` distribution but materially different downstream test-pass rate) |
| **W6-C1 (SDK v3.7; FALSIFIED on Phase-53)** | **Drafted conjecture: ``structure_gain`` (capsule_learned − substrate, accuracy_full) is preserved or grows when the underlying LLM scales up. Falsifier: ``structure_gain[35B] ≤ 0`` with ``substrate ≥ capsule_learned − 0.05``.** | **falsified-empirical** on Phase-53 default: ``structure_gain[14B] = -0.4``; ``structure_gain[35B] = 0.0``. Honest revised reading: structure_gain is **non-positive at every regime tested**; scale narrows a *deficit* (not a surplus). |
| **W6-C2 (SDK v3.7; FALSIFIED on Phase-53)** | **Drafted conjecture: the per-role admission scorer trained on Phase-52 synthetic+noise data transfers usefully (capsule_learned > capsule_fifo) to real-LLM candidate streams. Falsifier: capsule_learned beats capsule_fifo by < 0.05 on average across all model regimes.** | **falsified-empirical** on Phase-53 default: capsule_learned − capsule_fifo = -0.40 / -0.40 / 0.00 across (synthetic, 14B, 35B); average gap = -0.267. The W4-C1 (SDK v3.5) advantage **does not transfer OOD** to the real-LLM regime on this bench. |
| **W6-C3 (SDK v3.7)** | **36B-MoE producer roles emit a different candidate-handoff distribution than 14.8B-dense producer roles on the same scenario bank under matched prompt and temperature; falsifier: per-role TVD between candidate-kind histograms < 0.10.** | **empirical-research (positive)** — TVD = 0.167 on the pooled (source_role × claim_kind) histogram across 5 scenarios; modest but above the 0.10 falsifier; cf. W5-1's parser-axis TVD = 1.000 (the multi-agent extractor axis is more model-class-stable than the parser axis on the same hardware). |
| **W6-C4 (SDK v3.7; new)** | **Substrate FIFO admission is competitive with every capsule admission policy (FIFO, priority, coverage, learned) in real-LLM-driven multi-agent benchmarks at sufficient per-role budget. Falsifier: a (model, scenario, K_auditor) configuration where ``substrate_acc[m, s] < min_capsule_acc[m, s] - 0.05``.** | **conjectural-empirical** — anchored by 0.000 gap at K_auditor=4 on Phase-53 default; falsifier search direction = lower K_auditor (1, 2, 3) where substrate FIFO must admit non-causal head-of-arrival emissions. |
| **W6-C5 (SDK v3.7; new)** | **Model scale narrows the OOD generalisation gap of the per-role admission scorer trained on synthetic noise: ``scale_gain[capsule_learned] > 0 ≥ scale_gain[every fixed capsule policy] = 0`` whenever the synthetic training distribution carries non-trivial noise (``spurious_prob ≥ 0.20``). Falsifier: a synthetic→real transfer where ``scale_gain[capsule_learned] ≤ 0``.** | **conjectural-empirical** — anchored by ``scale_gain[capsule_learned] = +0.400`` against ``scale_gain[fixed] = 0`` on Phase-53 default. |
| **W7-C1 (SDK v3.8; new)** | **Multi-service-gold extension: a cohort policy that admits the *top-2* plurality tags (rather than the strict plurality) strictly improves accuracy_full on multi-service-gold scenarios over single-plurality cohort. Falsifier: a multi-service Phase-55-style bench where top-2 cohort ties or loses to single-plurality cohort.** | **conjectural** (Phase-55 candidate; not yet measured). |
| **W7-C2 (SDK v3.8; new)** | **Bundle-aware decoder + cohort admission strictly outperforms cohort admission alone on benches with weak / partial service-tag coherence (e.g. some gold candidates lacking a service tag). Falsifier: a bench where the decoder modification provides ≤ +0.05 on accuracy_full over cohort_buffered alone.** | **conjectural** (decoder modification not yet shipped). |
| **W7-C3 (SDK v3.8; new)** | **The W7-2 structural win transfers to a real-LLM regime when the LLM is prompted with a multi-service event mix (foreign-service decoys come from the prompt, not from synthetic injection). Falsifier: a real-LLM run where buffered cohort coherence achieves ``cohort_buffered − fifo accuracy_full < 0.20`` after surplus is verified.** | **conjectural** (Phase-56 candidate; requires Mac-2 or wider real-LLM runs). |
| **W8-C1 (SDK v3.9; new)** | **Multi-service-gold extension: a corroboration policy that admits the *top-k* highest-scoring tags strictly improves accuracy_full on multi-service-gold scenarios over single-tag corroboration on a Phase-56-style bench. Falsifier: a multi-service bench where top-k corroboration ties or loses to single-tag corroboration.** | **conjectural** (Phase-56 candidate; not yet measured). |
| **W8-C2 (SDK v3.9; new)** | **The W8-1 strict-separation result transfers to a real-LLM regime when the LLM is prompted with a multi-service event mix that produces decoy raw plurality + gold cross-role corroboration in the LLM-extracted candidate stream. Falsifier: a real-LLM run where corroboration beats FIFO by < 0.20 on accuracy_full after both bench properties are verified.** | **conjectural** (Phase-56 candidate; SDK v3.9 confirms no-regression in low-surplus 14B regime, but the W8-1 conditional regime requires re-prompting). |
| **W8-C3 (SDK v3.9; new)** | **Bundle-aware decoder + corroboration admission strictly outperforms corroboration alone on benches where admission cannot fully filter foreign-service candidates (e.g. partial-coverage cases). Falsifier: a bench where the decoder modification provides ≤ +0.05 over admission-alone.** | **conjectural** (decoder modification not yet shipped; restates W7-C2 under W8 framing). |
| **W8-C1 (SDK v3.9; DISCHARGED by W9-1 in SDK v3.10)** | **Originally: a corroboration policy that admits the *top-k* highest-scoring tags strictly improves accuracy_full on multi-service-gold scenarios over single-tag corroboration on a Phase-56-style bench.** | **DISCHARGED-empirical** by W9-1 on Phase 56: top-K with argmax-by-role-count gate beats single-tag W8 by +1.000 stable across 5/5 bank seeds. |
| **W9-C1 (SDK v3.10; DISCHARGED by W10-1 in SDK v3.11)** | **Originally: bundle-aware decoder companion that filters service tags at decode time by the dominant (claim_kind, role) signature strictly improves accuracy_full on the Phase-56 falsifier regime where admission alone admits a decoy.** | **DISCHARGED-empirical** by W10-1 on the Phase-57 decoder-forcing regime: bundle-aware decoder paired with W9 admission (top_k=3) beats every service-blind admission policy by +1.000 stable across 5/5 bank seeds. The decoder-side axis is now the load-bearing axis of the SDK v3.11 milestone. |
| **W9-C2 (SDK v3.10; new)** | **Top-K extension to |gold_services| ≥ 3: the W9-1 strict-separation result generalises to multi-service incidents with ``|gold_services| = N ≥ 3`` when ``top_k = N``, provided every gold service is corroborated by ≥ 2 distinct producer roles AND every decoy is corroborated by ≤ 1. Falsifier: a Phase-57 bench (3-service gold) where W9 with ``top_k=3`` beats FIFO by < 0.50 on accuracy_full.** | **conjectural** (Phase-57 bench not yet built; the W9 policy already supports arbitrary top_k). |
| **W9-C3 (SDK v3.10; new)** | **The W9-1 strict-separation result transfers to a real-LLM regime when the LLM is prompted with a multi-service event mix that produces multi-service-gold corroboration in the LLM-extracted candidate stream. Falsifier: a real-LLM run where multi_service beats W8 by < 0.20 on accuracy_full after the multi-service-gold property is verified.** | **conjectural** (Phase-58 candidate; SDK v3.10 confirms no-regression in low-surplus synthetic regime). |
| **W10-C1 (SDK v3.11; new)** | **CCK-table extension: the bundle-aware decoder approach generalises to non-incident-triage benchmark families when the family admits a closed-vocabulary tier mapping `claim_kind → tier` and `root_cause → tier`. Falsifier: a benchmark family where no closed-vocabulary tier mapping exists OR where the chosen `root_cause` does not entail a tier (e.g. cross-domain incidents).** | **conjectural** (closed-vocabulary tier mapping for security-incident, robotics, or compliance-review families not yet shipped). |
| **W10-C2 (SDK v3.11; new)** | **Real-LLM transfer of W10-1: when the producer LLM emits a multi-service-gold + corroborated-decoy stream, pairing W9 admission with the bundle-aware decoder closes the W10-Λ gap on the LLM-extracted candidate stream. Falsifier: a real-LLM run where bundle_decoder beats max(admission-only) by < 0.50 on accuracy_full after both bench properties are verified.** | **conjectural** (Phase-58 candidate; requires a real-LLM extractor that produces the bench properties). |
| **W10-C3 (SDK v3.11; PARTIALLY DISCHARGED by W11-1 in SDK v3.12)** | **Originally: multi-round bundle decoder closes W10-4 (decoy-CCK-promotion) on a sub-class of scenarios.** | **PARTIALLY DISCHARGED-empirical** by W11-1 on R-58 (a different sub-class — *delayed-causal-evidence* with noise-corroborated decoy — than W10-4's CCK-promoted decoy). The original W10-4 sub-class (decoy mentioned via causal-tier kind) remains open. The W11 contradiction-aware drop is orthogonal to W10's CCK predicate. |
| **W11-C1 (SDK v3.12; new)** | **The noise-decoy drop generalises to non-incident-triage benchmark families when the family admits a closed-vocabulary `generic-noise` claim_kind set. Falsifier: a benchmark family where a generic-noise-only mention is informative for the gold service set OR where no closed-vocabulary noise-set exists.** | **conjectural** (closed-vocabulary noise-set for security-incident, robotics, or compliance-review families not yet shipped). |
| **W11-C2 (SDK v3.12; PARTIALLY DISCHARGED by W12-1 in SDK v3.13)** | **Originally: real-LLM transfer of W11-1: when round-1 producers emit generic-tier observations and round-2 producers emit specific-tier diagnoses on the LLM-extracted candidate stream, multi_round closes the W11-Λ gap.** | **PARTIALLY DISCHARGED-empirical** by W12-Λ + W12-1: the *un-normalised* W11 decoder does NOT transfer (W12-Λ shows multi_round ties FIFO at 0.000 on Phase-59 default at ``synonym_prob=0.50``), but a *normalised* W11 decoder (W12-1) DOES transfer (+1.000 vs every un-normalised method, stable 5/5). The honest revised reading is: synthetic cross-round structure transfers to real-LLM regimes *only when an explicit normalisation layer absorbs the producer's kind / payload drift channel*. The original conjecture (W11 alone transfers) is **falsified-empirical**; the closure-conditional generalisation (W11 + closed-vocabulary normalisation transfers under bounded noise) is **W12-1**, proved-conditional + proved-empirical. |
| **W11-C3 (SDK v3.12; new)** | **Multi-step disambiguation: with three or more rounds and conflicting specific-tier evidence across rounds (e.g. round-2 emits DEADLOCK on gold_A, round-3 emits POOL_EXHAUSTION on gold_B), a contradiction-aware round-resolution rule (last-wins / weighted-confidence) strictly outperforms naive union. Falsifier: a 3-round bench where any contradiction-aware rule provides ≤ +0.05 over naive union on accuracy_full.** | **conjectural** (multi-step capsule chains not yet shipped). |
| **W12-C1 (SDK v3.13; new)** | **Cross-bench transfer of the W12 normalisation contract: the closed-vocabulary normalisation pattern generalises to non-incident-triage benchmark families when the family admits a closed-vocabulary closure of LLM kind variants. Falsifier: a benchmark family (security-incident, robotics, compliance-review) where the LLM's kind drift cannot be enclosed by any reasonable-size synonym table (i.e. the variant set is unbounded).** | **conjectural** (closed-vocabulary closure for security-incident, robotics, or compliance-review families not yet shipped). |
| **W12-C2 (SDK v3.13; new)** | **Real-Ollama (qwen2.5:14b-32k or qwen3.5:35b) producer transfer of W12-1: when the producer LLM is a real Ollama-served model on the Phase-53-style prompt, the same R-59 strong bar (gap robust − w11 ≥ 0.50, audit_ok preserved, no regression on R-53..R-58) holds. Falsifier: a real-Ollama run where the LLM emits drift outside :data:`CLAIM_KIND_SYNONYMS` on > 50% of causal claims (i.e. the synthetic noise channel is *narrower* than the real LLM's variant distribution), in which case W12-1 partially fails and the strong-bar advance is partial. The opt-in ``ollama`` LLM mode is the operator path; SDK v3.13 establishes the synthetic side of the transfer; the real-Ollama side is the next data point and the natural Mac-2-returns probe.** | **conjectural** (Phase-59 ``--llm-mode ollama`` candidate; requires Mac 1 or Mac 2 reachable + a willingness to accept synthetic-fallback contamination on HTTP failure). |
| **W12-C3 (SDK v3.13; PARTIALLY DISCHARGED by W13-1 in SDK v3.14)** | **Originally: an adaptive (learned or heuristic) synonym table closes more of the LLM-drift channel than the closed-vocabulary :data:`CLAIM_KIND_SYNONYMS`.** | **PARTIALLY DISCHARGED-empirical** by W13-1: a *heuristic* layered normaliser (exact + regex-predicate abstraction rules) strictly widens the closure on R-60-wide (every variant in :data:`HEURISTIC_RESCUABLE_OOV_KINDS` resolves to its named canonical, +1.000 vs W12). The *learned* normaliser variant remains conjectural (W13-C2). The closure boundary is structural, not the table contents (W13-4). |
| **W13-C1 (SDK v3.14; new)** | **Cross-bench transfer of the W13 closure-widening contract: the layered (exact + heuristic) normalisation pattern generalises to non-incident-triage benchmark families when the family admits a closed-vocabulary tier mapping AND a small set of regex predicates over kind text whose union covers ≥ 90 % of LLM kind drift on the family's prompt. Falsifier: a benchmark family (security-incident, robotics, compliance-review) where any size-bounded heuristic predicate set covers < 50 % of LLM drift on the calibrated prompt.** | **conjectural** (closed-vocabulary tier mapping + heuristic rule set for security-incident, robotics, or compliance-review families not yet shipped). |
| **W13-C2 (SDK v3.14; new)** | **Learned-normaliser closure: an embedding-distance-based or LLM-distilled normaliser strictly widens the W13 heuristic closure on R-60-cosmic. Specifically, a learned normaliser that maps OOV kinds to their nearest canonical via cosine distance over a small claim-kind embedding survives a higher ``cosmic_oov_prob`` than the regex-predicate set. Falsifier: a learned normaliser that does not strictly improve over the W13 heuristic table at ``cosmic_oov_prob ∈ {0.30, 0.50, 0.70}``. Honest scope: this is a closure-widening move, not a structural fix — the W13-4 closure boundary applies to any predicate-based normaliser; a learned normaliser only widens the closure but does not eliminate it (a maximally adversarial drift channel is still a finite-VC-dim adversary). | **conjectural** (no learned normaliser shipped; the heuristic table is the SDK v3.14 anchor). |
| **W13-C3 (SDK v3.14; new)** | **Real-Ollama transfer of W13-1 with a redesigned event stream: when the Phase-58 events are redesigned so the decoy and gold services have *comparable* magnitudes (so a real LLM at the 14B class does not filter the decoy as noise) AND the prompt instructs the LLM to emit one claim per distinct event, real Ollama qwen2.5:14b-32k does emit the R-58 delayed-causal-evidence shape AND does drift kinds with non-trivial probability (≥ 20 %); under this regime, the W13-1 strict bar (gap ≥ 0.50) is cleared on the real-LLM stream. Falsifier: a real-Ollama run on the redesigned events where W13 − W12 < 0.20 pooled accuracy_full. Honest scope: this is the natural fix for the W13-Λ-real observation — but it pushes the contribution back onto event-shape / prompt-side rather than on the normaliser per se.** | **conjectural** (Phase-60 v2 candidate; requires a redesigned event stream that elicits non-trivial drift from a 14B-class LLM). |
| **W13-C4 (SDK v3.14; new)** | **Abstention as a load-bearing signal: when the W13 normaliser is configured with ``abstain_on_unknown=True``, the priority decoder can use the abstention rate as an "I don't know" signal and trigger a fallback (e.g. a different admission policy, a request for additional rounds, an LLM rewrite call). On a regime where the OOV rate is non-trivial but bounded, an abstention-aware decoder strictly improves over a passthrough decoder. Falsifier: a regime where the abstention-aware decoder ties or loses to the passthrough decoder by ≥ 0.05 on accuracy_full.** | **conjectural** (the abstention sentinel is implemented; the abstention-aware decoder pathway is not yet wired). |
| **W13-C3 (SDK v3.14; PARTIALLY DISCHARGED by W14-1 + W14-Λ-real in SDK v3.15)** | **Originally: real-Ollama transfer of W13-1 with a redesigned event stream + a prompt that instructs the LLM to emit one claim per distinct event would clear the W13-1 strong bar (gap ≥ 0.50) on the real-LLM stream.** | **PARTIALLY DISCHARGED-empirical** by W14-1 + W14-Λ-real: the *combination* of comparable-magnitude events + structured producer protocol clears the strict-gain bar on real Ollama 14B (n=8, +0.500 vs FIFO at exactly the R-61-OLLAMA-A threshold), BUT the gain is on the **W11/W12/W13 cross-round pipeline as a whole** — not specifically on the W13 closure-widening, which is structurally invisible because the real LLM still emits canonical kinds. The honest revised reading: prompt-side discipline + comparable-magnitude events restore the bench property; W13's *normalisation* contribution remains dormant on this regime. The "specifically on W13" framing of the original W13-C3 is **falsified-empirical** in the negative direction (no kind drift to widen the closure of); the cross-round structural bar is cleared at +0.500 by the combined intervention (W14). |
| **W14-C1 (SDK v3.15; new)** | **Cross-bench transfer of the W14 protocol: the layered observation/diagnosis prompt schema generalises to non-incident-triage benchmark families when the family admits an observation/diagnosis tier partition over its closed-vocabulary claim kinds AND the events fit a multi-round delayed-causal-evidence shape. Falsifier: a benchmark family (security-incident, robotics, compliance-review) where observation and diagnosis cannot be cleanly partitioned, OR where the round-1/round-2 separation is structurally absent.** | **conjectural** (only the incident-triage schema ships; security-incident / robotics / compliance-review schemas not yet built). |
| **W14-C2 (SDK v3.15; new)** | **Model-side magnitude calibration: the 1/8 R-61-ollama-structured failure (LLM judged ``error_rate=0.15`` not to qualify as ``ERROR_RATE_SPIKE``) is closed by an explicit *magnitude hint* extension to the structured prompt — the tier banner enumerates the magnitude thresholds the LLM should treat as qualifying. Under the magnitude-hinted prompt, qwen2.5:14b on R-61 events achieves ``accuracy_full ≥ 0.875``. Falsifier: a magnitude-hinted run where the model still under-emits gold claims at the same rate.** | **conjectural** (the magnitude hint is not yet wired into the structured prompt rendering; the W14-Λ-real 7/8 result is the empirical anchor in the *positive* direction, the failure scenario is the candidate falsifier). |
| **W14-C3 (SDK v3.15; new)** | **Multi-round generalisation: the W14 protocol's observation/diagnosis split generalises to N ≥ 3 rounds with a *graded* tier hierarchy (observation → preliminary diagnosis → refined diagnosis). Under a 3-round bench with two specific-tier kinds emitted on different rounds (round 2 = preliminary; round 3 = refined), the structured protocol with N-tier banners strictly outperforms the 2-round protocol on accuracy_full. Falsifier: a 3-round bench where the N-tier protocol provides ≤ +0.05 over the 2-round protocol on accuracy_full.** | **conjectural** (multi-step capsule chains not yet shipped; restates W11-C3 under the W14 framing). |
| **W14-C4 (SDK v3.15; new)** | **Cross-model transfer: the W14-1 win at qwen2.5:14b-32k transfers to qwen3.5:35b-MoE (the prior W5-1 cross-model probe) and to non-Ollama backends (MLX-distributed). Specifically, the structured protocol's bench-property recovery on R-61-ollama-structured ≥ 75 % holds for both model classes. Falsifier: a 35B run where the bench property holds in < 50 % of scenarios under the structured prompt.** | **conjectural** (requires Mac 2 reachable + a willingness to consume the wall-time budget; the SDK v3.15 anchor is 14B only). |
| **W14-C5 (SDK v3.15; new)** | **Multi-hypothesis variant: an extension of the structured protocol that explicitly permits multi-hypothesis listings on ambiguous round-1 events ("if uncertain, list 2-3 candidate kinds comma-separated") strictly improves accuracy_full over the single-hypothesis variant on regimes where the LLM's per-event confidence is low. Falsifier: a regime where the multi-hypothesis variant ties or loses to the single-hypothesis variant by ≥ 0.05.** | **conjectural** (not yet wired; the W14 default is single-hypothesis per event). |
| **W15-C-real (SDK v3.16; new)** | **Real-LLM downstream-decoder transfer of W15-1: the W15 salience pack provides a strict accuracy gain over FIFO truncation when the cross-round bundle is fed to a real LLM (e.g. Mac-1 ``qwen2.5:14b-32k``) configured as a downstream re-decoder agent under a tight context window. Specifically, on R-62-tightbudget with the bundle re-decoded by the LLM under a context window equal to ``T_decoder``, the W15 method maintains ≥ 0.20 strict accuracy gain over FIFO truncation. Falsifier: a real-LLM downstream-decoder run where W15 − FIFO < 0.10 OR where the LLM's attention is sufficiently uniform that prompt-position ordering does not change correctness (the proxy attention metric does not transfer to the real model).** | **conjectural** (requires Mac 1 / Mac 2 reachable + an LLM downstream re-decoder pipeline; SDK v3.16 ships the synthetic side only). |
| **W15-C1 (SDK v3.16; new)** | **Cross-bench transfer of the W15 packing contract: the salience scoring pattern (tier + CCK + corroboration + magnitude + round) generalises to non-incident-triage benchmark families when the family admits a closed-vocabulary CCK + magnitude vocabulary. Falsifier: a benchmark family (security-incident, robotics, compliance-review) where no closed-vocabulary tier mapping or magnitude proxy exists; equivalently, where the salience score becomes uninformative (variance below the within-tag tie-break threshold).** | **conjectural** (only the incident-triage CCK + magnitude vocabulary ships; security-incident / robotics / compliance-review schemas not yet built). |
| **W15-C-LEARNED (SDK v3.16; new)** | **Learned salience: a logistic-regression / small MLP scorer over per-handoff features (analogous to :class:`LearnedTeamAdmissionPolicy`) outperforms the closed-form W15 weights on a held-out test set across 5/5 random splits. Falsifier: any held-out split where the learned salience ties or loses to the closed-form weights by ≥ 0.05. Honest scope: the learned variant is at risk of OOD failure analogous to W4-C1 / W6-C1 / W6-C2; the closed-form weights remain the SDK v3.16 anchor.** | **conjectural** (no learned salience shipped; W15 default is closed-form). |
| **W15-C-SYMMETRIC (SDK v3.16; DISCHARGED-NEGATIVE by W17-Λ-symmetric in SDK v3.18)** | **Originally: Symmetric-corroboration multi-hypothesis structural limit; the natural next-axis open question for SDK v3.17+.** | **DISCHARGED as a negative theorem** by W17-Λ-symmetric on R-64-SYM (SDK v3.18). The construction shows every capsule strategy in the SDK ties FIFO at 0.000 under both ``T_decoder ∈ {None, 24}`` by construction; the priority decoder still elects the right specific-tier root_cause but ``services_correct`` set-equality fails because the W11 contradiction-aware drop fires symmetrically. **The wall is real and structural** — escape requires a learned or LLM-distilled semantic-disambiguator beyond the closed-form capsule surface (W17-C-DISAMBIGUATOR, conjectural). |
| **W15-C-COMPOSE-W14 (SDK v3.16; PARTIALLY DISCHARGED by W16-Λ-real-replay in SDK v3.17)** | **Originally: W15 + W14 compose on real Ollama; running W15 over the W14-emitted stream closes the 1/8 model-error failure W14 alone leaves, achieving ``accuracy_full ≥ 0.625``.** | **PARTIALLY DISCHARGED-empirical** by W16-Λ-real-replay in SDK v3.17. The replay path over the recorded ``phase61_real_ollama_structured_qwen2_5_14b_n8.json`` bytes shows the composition delivers a strict +0.500 gain over the FIFO-packed-W14-only baseline at T_decoder=14 — but does NOT close the 1/8 model-error failure (composed accuracy_full = 0.500 = W14-only loose-budget accuracy). The composition is *robust to budget pressure* (the recovered W14 anchor) but does not *transcend* the upstream model-side judgment failure. The honest revised reading: W15 + W14 compose by making the W14 result *robust to bounded context*; closing the 1/8 model-side failure remains W16-C-LIVE-OLLAMA / W14-C2 (magnitude-hint extension), conjectural. |
| **W16-C1 (SDK v3.17; new)** | **Cross-bench transfer of the W14+W15 composition: the layered composition pattern (structured producer protocol + salience-aware decoder packing under T_decoder) generalises to non-incident-triage benchmark families (security-incident / robotics / compliance-review) when the family admits both an observation/diagnosis tier partition AND a closed-vocabulary CCK + magnitude vocabulary AND multi-hypothesis events with asymmetric corroboration. Falsifier: a benchmark family where one of the two layers is structurally absent (e.g. a single-round bench has no W14 round-1/round-2 split; a single-hypothesis bench has no W11-drop trigger).** | **conjectural** (only the incident-triage schema + CCK + magnitude vocabulary ships; cross-bench schemas not yet built). |
| **W16-C-LIVE-OLLAMA (SDK v3.17; DISCHARGED by W17-1 in SDK v3.18)** | **Originally: a fresh live Ollama probe under R-63-COMPOSED-TIGHT closes the 1/8 model-error failure W14-only leaves, achieving ``accuracy_full ≥ 0.625``.** | **DISCHARGED-empirical** by W17-1 on R-64-LIVE-MAGHINT (SDK v3.18). The fresh live probe achieves ``accuracy_full = 1.000`` (closes the 1/8 miss entirely; bench property holds in 8/8) — but the load-bearing intervention is *not* the live re-run alone (the W17-Λ-no-hint anchor on R-64-LIVE-STRUCT reproduces the recorded 0.500 envelope on the fresh probe). The magnitude-hint extension (W17) is what closes the gap from 0.500 to 1.000. The original conjecture's premise (a live re-run alone closes the gap) is **falsified-empirical**; the closure-conditional generalisation (a live re-run + magnitude-hint extension closes the gap) is **W17-1**, proved-conditional + empirical-research. |
| **W16-C-CROSS-MODEL (SDK v3.17; PARTIALLY DISCHARGED by W17-C-XMODEL in SDK v3.18)** | **Originally: cross-model transfer of the composition to qwen3.5:35b under MLX-distributed inference.** | **PARTIALLY DISCHARGED-empirical** by W17-C-XMODEL on a fresh live ``qwen3.5:35b`` Ollama probe (NOT MLX-distributed; the simpler Ollama backend with ``think = False`` was reachable on Mac-1) under the W17 magnitude-hinted prompt: bench property holds in 8/8; ``capsule_attention_aware = 0.750``; +0.750 strict gain over FIFO-pack and substrate FIFO. The strict-gain clause is met; the saturated full-correctness clause (1.000 on 35B) remains conjectural and bounded by a 35B-specific specific-tier judgment artifact. The MLX-distributed clause of the original W16-C-CROSS-MODEL (vs the Ollama backend on Mac-1) is *orthogonal* to W17-C-XMODEL and remains conjectural pending Mac-2 reachable. |
| **W16-C-LEARNED-COMPOSE (SDK v3.17; new)** | **Learned salience + learned producer: a learned salience scorer (W15-C-LEARNED) plus a learned producer protocol (W14-C5 multi-hypothesis variant) outperforms the closed-form W16 composition on a held-out test set across 5/5 random splits. Falsifier: any held-out split where the learned composition ties or loses to the closed-form composition by ≥ 0.05.** | **conjectural** (no learned salience and no multi-hypothesis producer variant shipped; the closed-form composition remains the SDK v3.17 anchor). |
| **W16-C-SYMMETRIC (SDK v3.17; DISCHARGED-NEGATIVE by W17-Λ-symmetric in SDK v3.18)** | **Originally: Symmetric-corroboration limit on the composed regime; SDK v3.18+ candidate.** | **DISCHARGED as a negative theorem** by W17-Λ-symmetric on R-64-SYM (SDK v3.18). See the W15-C-SYMMETRIC discharge above; W16-C-SYMMETRIC is the same wall stated under the W16 framing. **The wall is real and structural** for the closed-form capsule surface; escape requires a learned or LLM-distilled semantic-disambiguator (W17-C-DISAMBIGUATOR, conjectural). |
| **W17-C-DISAMBIGUATOR (SDK v3.18; new)** | **Semantic-disambiguator beyond the closed-form capsule surface: a learned or LLM-distilled disambiguator that consumes the round-2 disambiguator's evidence text directly can in principle distinguish ``orders_payments_join`` (gold A_B in deadlock) from a generic decoy whose round-1 mentions are observationally identical. The natural research move beyond the W17-Λ-symmetric wall.** | **conjectural** (out of scope for SDK v3.18; named research frontier for SDK v3.19+). |
| **W17-C-LEARNED-HINT (SDK v3.18; new)** | **Learned threshold variant: a per-kind data-driven threshold table outperforms the closed-form :data:`INCIDENT_TRIAGE_DEFAULT_MAGNITUDE_THRESHOLDS` on a held-out test set across 5/5 random splits. Falsifier: any held-out split where the learned threshold ties or loses to the closed-form table by ≥ 0.05.** | **conjectural** (out of scope for SDK v3.18; the closed-form table is the W17-1 anchor). |
| **W17-C-CROSS-BENCH (SDK v3.18; new)** | **Cross-bench transfer of the magnitude-hint extension: the W17 protocol generalises to non-incident-triage benchmark families when the family admits a closed-vocabulary kind → qualifying-threshold mapping AND the LLM is observed to make relative-magnitude judgments on its events. Falsifier: a benchmark family where the bench property cannot be made to hold under any reasonable threshold table on the calibrated prompt.** | **conjectural** (out of scope for SDK v3.18; only the incident-triage threshold table ships). |

## Bundle-relational compatibility disambiguator (W18-Λ-sym, W18-Λ-no-compat, W18-Λ-confound, W18-Λ-deceive, W18-1..W18-3, W18-C-LEARNED, W18-C-OUTSIDE, W18-Λ-real) — SDK v3.19

| Claim                | Description                                                                                                                                                                         | Status                            | Anchor                                                                                                |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **W18-Λ-sym (SDK v3.19; extension of W17-Λ-symmetric)** | **W17-Λ-symmetric extends to R-65-COMPAT verbatim for every method pre-W18: on the R-65-COMPAT bench (every gold service AND the decoy mentioned by ≥ 2 distinct routed roles via generic-noise kinds with comparable magnitudes — same shape as R-64-SYM), every closed-form salience scorer in the SDK — substrate FIFO, ``capsule_fifo``, ``capsule_priority``, ``capsule_coverage``, W7-2 cohort, W8 corroboration, W9 multi-service, W11 multi-round, W12 robust-multi-round, W13 layered, W15 ``AttentionAwareBundleDecoder``, W14H + W15 composition — ties FIFO at ``accuracy_full = 0.000``. The bipartite ``(role × tag, kind, magnitude)`` multiset is identical for gold and decoy by construction; no service-blind admission AND no closed-form salience packer can prefer one over the other.** | **proved-empirical (n=8 saturated × 5 seeds) + structural sketch** | `docs/data/phase65_cross_regime_synthetic.json::r65_compat_loose.pooled` (every non-W18 strategy at 0.000); `docs/data/phase65_seed_sweep_loose.json` (5/5 seeds reproduce); structural argument is the same as W17-Λ-symmetric extended by construction over the W17 + W14H + W15 surface. |
| **W18-1 (SDK v3.19)** | **Bundle-relational compatibility disambiguator sufficiency on the symmetric-corroboration regime where the round-2 specific-tier disambiguator carries a relational-compound mention of every gold service AND no decoy service: pairing the W15 ``AttentionAwareBundleDecoder`` with the new ``RelationalCompatibilityDisambiguator`` (W18: tokenises the round-2 disambiguator's payload, scores each admitted service tag by direct-match + contiguous-subsequence compound match, projects the W11/W15 answer through the strict-asymmetric branch when at least one but not all admitted tags have positive score) achieves ``accuracy_full = 1.000`` on R-65-COMPAT-LOOSE (``T_decoder = None``) AND R-65-COMPAT-TIGHT (``T_decoder = 24``), strictly improving over every non-W18 capsule baseline by ``+1.000`` on both, stable across 5/5 alternate ``bank_seed`` values (11, 17, 23, 29, 31). Min gap ``w18 − attention_aware = +1.000`` per seed (saturated; well above the 0.50 strong-bar threshold). The first capsule-native multi-agent-coordination method that crosses the symmetric-corroboration wall on a regime where the wall actually applies.** | **proved-conditional + proved-empirical (n=40 saturated across 5 seeds × 2 budgets)** | `docs/data/phase65_cross_regime_synthetic.json::r65_compat_loose,r65_compat_tight`; `docs/data/phase65_seed_sweep_loose.json`, `phase65_seed_sweep_tight.json`; `Phase65DefaultConfigTests::test_w18_strict_win_loose,..._tight`; `Phase65SeedStabilityTests::test_loose_min_gap_above_strong_bar,..._tight`. |
| **W18-2 (SDK v3.19)** | **W18 determinism + closed-form correctness (proved by inspection + mechanically-checked): (a) ``_disambiguator_payload_tokens`` is byte-deterministic on payload bytes; (b) ``_relational_compatibility_score`` is byte-deterministic given the tokens and target; (c) the W18 strict-asymmetric branch fires *iff* at least one but not all admitted tags have positive score AND falls through to the inner answer otherwise (W18-3 backward-compat); (d) the W18 method consumes only the W15-packed bundle — no extra capsule reads, no global state — so token-budget accounting is byte-for-byte preserved (bounded-context honesty).** | **proved (by inspection) + mechanically-checked** | `vision_mvp/wevra/team_coord.py::RelationalCompatibilityDisambiguator,_disambiguator_payload_tokens,_relational_compatibility_score`; `W18ScorerUnitTests`, `W18DecoderUnitTests`, `Phase65TokenEfficiencyTests`. |
| **W18-3 (SDK v3.19)** | **Backward-compat with R-54..R-64 (proved-empirical full programme regression): on R-58 default the W18 method ties ``AttentionAwareBundleDecoder`` byte-for-byte on the answer field — either via abstention (no relational mention in round-2) or via strict-asymmetric projection that lands on the same gold-only set the W11 contradiction-aware drop produces. On R-64-SYM the W18 method partially recovers (only the deadlock scenarios carry a relational mention; the others abstain and tie FIFO), an honest partial-success reading that motivates the R-65-COMPAT bench as the *consistent* relational-mention regime. The W18 surface is purely additive on top of the W15 surface; with ``enabled = False`` it reduces to W15 byte-for-byte.** | **proved-empirical (cross-regime audit)** | `Phase65BackwardCompatTests::test_w18_matches_w15_on_phase58_deadlock,..._partial_recovery_on_p64sym_deadlock`; `W18DecoderUnitTests::test_disabled_path_reduces_to_inner`. |
| **W18-Λ-no-compat (SDK v3.19; new)** | **W18-Λ-no-compat falsifier (proved-empirical n=8 saturated): on the R-65-NO-COMPAT bank where round-2 disambiguator's payload contains the kind-token + numeric magnitudes but NO service-tag mention, the W18 compatibility score is uniformly zero across every admitted tag; W18 abstains and falls through to the inner answer (which itself ties FIFO at 0.000 by W17-Λ-symmetric). ``accuracy_full = 0.000`` on 8/8 cells. Names the structural limit of any closed-form bundle-relational compatibility scorer when round-2 evidence carries no relational signal.** | **proved-empirical (n=8 falsifier saturated) + structural sketch** | `docs/data/phase65_cross_regime_synthetic.json::r65_no_compat`; `Phase65FalsifierTests::test_w18_lambda_no_compat_ties_fifo`; structural: every per-tag score is 0 → strict-asymmetric branch cannot fire → abstention. |
| **W18-Λ-confound (SDK v3.19; new)** | **W18-Λ-confound falsifier (proved-empirical n=8 saturated): on the R-65-CONFOUND bank where round-2 disambiguator mentions BOTH gold AND decoy service tags, the W18 compatibility score is positive on every admitted tag; W18 abstains (no strict-asymmetric tie can be broken) and falls through to the inner answer, which ties FIFO at 0.000 by W17-Λ-symmetric. ``accuracy_full = 0.000`` on 8/8 cells. Names the structural limit when round-2 evidence is itself relationally-symmetric — a closed-form scorer cannot prefer gold over decoy when both are equally named.** | **proved-empirical (n=8 falsifier saturated) + structural sketch** | `docs/data/phase65_cross_regime_synthetic.json::r65_confound`; `Phase65FalsifierTests::test_w18_lambda_confound_ties_fifo`. |
| **W18-Λ-deceive (SDK v3.19; new)** | **W18-Λ-deceive falsifier (proved-empirical n=8 saturated): on the R-65-DECEIVE bank where round-2 disambiguator mentions DECOY service tags but NOT gold, W18 trusts its evidence and projects the answer to the decoy-only set; ``services_correct = False`` because gold is missing. ``accuracy_full = 0.000`` on 8/8 cells. Names the structural limit when round-2 evidence is adversarially-relational; no closed-form bundle-relational scorer that *trusts* the disambiguator's payload can escape this regime without an outside-information axis (W18-C-OUTSIDE, conjectural).** | **proved-empirical (n=8 falsifier saturated) + structural sketch** | `docs/data/phase65_cross_regime_synthetic.json::r65_deceive`; `Phase65FalsifierTests::test_w18_lambda_deceive_picks_decoy_and_fails`. |
| **W18-C-LEARNED (SDK v3.19; new)** | **Learned bundle-relational scorer: a small distilled compatibility scorer (e.g. a character-bigram model + relational-compound embedding lookup, trained on a few hundred synthetic disambiguator-payload / gold-tag pairs) outperforms the closed-form mention-and-relational-compound rule on a held-out cross-bench when the LLM emits free-form relational mentions outside the synthetic bench's exact-match closure (e.g. natural-language ``"the join between orders and payments"``). Falsifier: a held-out cross-bench where the learned scorer ties or loses to the closed-form rule by ≥ 0.05 across 5/5 random splits.** | **conjectural** (out of scope for SDK v3.19; the closed-form scorer is the W18-1 anchor). |
| **W18-C-OUTSIDE (SDK v3.19; new)** | **Outside-information escape from W18-Λ-deceive: a scorer with access to an outside-information axis (e.g. service-graph topology, prior reliability scores, cross-incident historical evidence) can detect the W18-Λ-deceive regime by cross-referencing the round-2 mention against an outside source and rejecting the projection when the cross-reference fails. Falsifier: a hand-constructed adversarial setting where the outside source is itself compromised AND the deceptive round-2 mention is internally consistent with the outside source. Honest cap: even with outside information, an adversarial round-2 + adversarial outside source jointly bound the closed-form scorer's reach.** | **conjectural** (out of scope for SDK v3.19; named structural escape from the W18-Λ-deceive falsifier). |
| **W18-Λ-real (SDK v3.19; new)** | **Real-LLM transfer of the W18 method: when a real LLM produces the round-2 disambiguator (instead of the synthetic ``IdentityExtractor``), the W18 strict gain on R-65-COMPAT transfers iff the LLM emits the same relational-compound forms the synthetic bench uses. Honest scope: free-form natural-language relational mentions (e.g. ``"the join between orders and payments"``) fall *outside* the W18 exact-match closure by construction; W18-Λ-real is partially closed when the LLM is prompted to emit closed-vocabulary relational compounds (analogous to W14 structured-prompt) and conjectural otherwise. Falsifier: a real-LLM probe on R-65-COMPAT where the LLM emits relational mentions outside the closure on > 30 % of cells AND W18 ties FIFO at 0.000 on the corresponding cells.** | **proved-conditional + empirical-research / conjectural-empirical-on-Mac-1** (Mac-1 reachable + the LLM's relational-mention emission style observed). |
| **W18-C-CROSS-BENCH (SDK v3.19; new)** | **Cross-bench transfer of the bundle-relational compatibility scorer: the W18 method generalises to non-incident-triage benchmark families when the family admits a closed-vocabulary relational-mention convention in its round-2 disambiguator (e.g. distributed-tracing spans, concurrency-bug witnesses, multi-service deployment manifests). Falsifier: a benchmark family where round-2 evidence does not carry any binary or n-ary relational compound between admitted entities AND no closed-form scorer can produce a strict gain over the W17 baseline.** | **conjectural** (out of scope for SDK v3.19; only the incident-triage relational-mention closure ships). |

## Phase-substrate (P19, P31, P35, P36, P39..P44)

The substrate-side phase-by-phase theorems (P19-L2, P31-1..P31-5,
P35-1..P35-3, P36-*, P39-*, P40-*, P41-*, P42-*, P43-*, P44-*)
are stated and proved in their respective `RESULTS_PHASE*.md`
notes. They are inputs to W3-11 (capsule subsumption) but are
not maintained in this registry — see `vision_mvp/RESULTS_PHASE*.md`
or `docs/archive/pre-wevra-theory/PROOFS.md`.

## How to add a claim

1. Pick the lowest unused W3-* / W3-C* number in the appropriate
   section.
2. Add the row here with status, one-line description, and
   anchor.
3. Add the formal statement to `docs/CAPSULE_FORMALISM.md` (for
   W3-* in the capsule algebra / runtime axes).
4. If empirical, add a falsifiability test to
   `vision_mvp/tests/test_wevra_*.py` or a research shard.
5. If conjectural, add the falsifier (what would refute it).
6. Cross-link from `docs/RESEARCH_STATUS.md`.
